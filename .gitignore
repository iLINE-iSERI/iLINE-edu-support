# Dependencies
/node_modules
/.pnp
.pnp.js

# Next.js
/.next
/out
next-env.d.ts

# Production
/build
/dist

# Environment — 값이 들어있는 파일은 어떤 확장자든 커밋 금지
.env
.env.local
.env.*.local
.env*.txt
!.env.local.example

# 서비스 계정 키 (절대 커밋 금지)
*-service-account*.json
serviceAccount*.json
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.idea
.vscode
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# TypeScript 증분 빌드 캐시
*.tsbuildinfo

# 내려받은 파일을 모아 두는 자리 — 저장소의 일부가 아니다.
#
# ⚠️ 여기에 소스 파일(.ts/.tsx) 사본이 들어오면 빌드가 깨진다.
#    2026-09-08에 실제로 그랬다: 이 폴더에 받아 둔 ApplicationForm.tsx 가
#    옆에 없는 './PortraitConsent' 를 불러서 Vercel 빌드가 실패했다.
#    타입 검사는 **저장소 안의 모든 .tsx** 를 보기 때문에, 쓰지 않는
#    사본이라도 문법이 맞아야 한다. 아예 저장소 밖으로 두는 것이 안전하다.
/Claude outputs/
