# 에이전트 활용 가이드 — 어떤 세션에 무엇을 맡기나

> **Claude 같은 개발 에이전트에게 이 사이트 일을 맡길 때** 어느 종류의 세션을 열고,
> 무엇을 어떻게 물어보고, 답을 어떻게 믿을지 정리한 문서입니다.
>
> 🔴 **코드를 몰라도 읽을 수 있게 썼습니다.** 에이전트가 파일 이름을 말할 때 그게
> 무엇인지는 `docs\claude 생성 파일 설명서.md` 를 보세요.
>
> 기준일: 2026-09-25

---

## 0. 세 줄 요약

1. 세션은 **두 종류**입니다 — **① 구현·운영**(급한 일) **② 유지보수**(미리 조금씩).
   ②는 항상 ①보다 우선순위가 낮습니다.
2. **①과 ②를 같은 시간에 돌려도 되지만**, 둘이 **같은 작업 폴더**를 쓰므로
   「고치기 전에 `git status`, 끝나면 바로 커밋」을 지켜야 섞이지 않습니다.
3. 에이전트는 **그럴듯하게 틀립니다.** 믿는 기준은 §5 에 있습니다.

---

## 1. 세션 두 종류 — 무엇을 맡기나

| | **① 구현·운영 세션** | **② 유지보수 세션** |
|---|---|---|
| 언제 | 기능이 필요할 때, 문제가 났을 때, 공고를 올려야 할 때 | 급한 일이 없는 시간에 |
| 무엇을 | 새 기능·버그 수정·문서 갱신·공고 준비 | 코드 정리, 구조 개선, 되풀이되는 사고의 원인 제거 |
| 회원에게 영향 | **있어도 된다** (그게 목적) | 🔴 **절대 없어야 한다** |
| 우선순위 | **높음** | **낮음. 언제든 중단** |
| 프롬프트 | 그때의 요청을 그대로 | **§2 의 긴 프롬프트를 붙여넣기** |

**②를 왜 따로 두나** — 사이트를 만드는 것만이 일이 아니고 **고쳐 가며 오래 쓰게 만드는
것까지**가 일입니다. 그런데 급한 일에 밀려 계속 미루게 되므로, 한가한 시간에 조금씩
떼어 놓으려고 세션을 나눕니다. **급한 일이 생기면 ②는 즉시 접습니다.**

---

## 2. 유지보수 세션 프롬프트 — 그대로 붙여넣기

새 세션을 열고 **아래 전체**를 붙여 넣습니다. 매번 새로 쓰지 않습니다.

> 💡 **날짜와 마감은 바뀝니다.** 「접수 마감」 표와 「최신 D 번호」는 붙여넣기 전에
> 지금 값으로 고쳐 주세요. 그 두 군데만 고치면 나머지는 그대로 씁니다.

````
# 유지보수 세션 — 파트별로 분석하고, 승인받으며 조금씩 리팩토링

너는 `aiedu.iline.or.kr`(교원양성기관 교육과정개발 지원사업 사이트)를 이어받았다.
Next.js 14 App Router · TypeScript · Tailwind · Firebase · Vercel.

이 세션의 일은 **사이트를 파트로 쪼개 하나씩 읽고, 개선할 점을 찾아, 승인을 받으며
조금씩 고치는 것**이다. 계획서를 쓰고 끝내는 세션이 아니라 **계획 → 승인 → 착수**를
파트마다 반복하는 세션이다.

---

## 0. 🔴 이 세션의 위치 — 먼저 이것부터 받아들여라

**이 작업은 우선순위가 낮다.** 사용자의 본업은 기능 구현과 운영이고, 그 작업은
**다른 세션**에서 진행된다. 이 세션은 *"유지보수·개선까지가 내 일이니 미리 조금씩
시작해 두자"* 는 것이다.

그래서:

- **구현 세션이 무언가를 필요로 하면 이 세션은 즉시 물러난다.** 하던 것을 커밋하거나
  되돌려서 **작업 폴더를 깨끗하게 비우고** 기다린다.
- **한 단계는 한 세션 안에 끝나야 한다.** 며칠 걸리는 변경을 시작하지 마라.
  중간 상태로 남겨 두면 구현 세션이 막힌다 — **그건 실패다.**
- 사용자가 *"이건 나중에"* 라고 하면 즉시 접고 기록만 남긴다. 설득하지 마라.

### ⚠️ 두 세션이 같은 작업 폴더를 쓴다

사용자는 다른 세션(cowork 쪽 포함)에서도 같은 폴더를 건드린다. 그래서:

- **파일을 고치기 전에 반드시 `git status`.** 다른 세션의 **미커밋 변경이 있는 파일은
  건드리지 마라.** 사용자에게 알리고 다른 파트로 넘어간다.
- **한 단계 끝나면 바로 커밋해서 트리를 비운다.** 사용자의 다음 세션 첫 작업이
  `git status` 다 — 네가 남긴 미커밋 변경이 그 세션을 혼란시킨다.
- 브랜치를 새로 만들지 마라. `main` 에 작은 커밋으로 쌓는다.

---

## 1. 🔴 절대 금지선 — 쓰고 있는 사람에게 영향이 가면 안 된다

**사이트는 지금 운영 중이고 실제 회원이 쓰고 있다.**

| 공고 | 접수 마감 | 상태 |
|---|---|---|
| 2026 데이터 디깅 캠프 | **9/30(수) 18:00** | 실제 신청 4건. 그 4명이 **마감 전까지 신청 내용을 고쳐야 한다** |
| AI-EDU Next Class 해커톤 | **10/6(화)** | 접수 중 |

담당자(교수님)도 지금 공고를 올리고 신청을 처리하는 중이다.

### 매 변경 전에 이 질문을 통과해야 한다

> **이 변경으로 ① 지금 로그인한 회원이나 담당자가 보는 화면이 한 군데라도
> 달라지는가 ② 저장되는 데이터의 모양이 달라지는가 ③ 제출·저장·동기화 경로가
> 달라지는가?**
>
> 하나라도 「그렇다」면 **지금 하지 않는다.** 기록만 남기고 다음으로 넘어간다.

### 접수가 끝나기 전에는 손대지 않는 것

- 회원이 보는 화면 전부 — 홈 · 프로그램 목록·상세 · **신청서** · 마이페이지 ·
  내 예약 · 1:1 문의 · 산출물
- 담당자 화면 전부 — 특히 `/staff/programs`(공고 수정)와 `/staff`(신청 목록).
  지금 실제 업무에 쓰인다
- `firestore.rules` · `storage.rules`
- 구글 시트·드라이브 동기화 (`lib/server/googleSync.ts`)
- 신청서 PDF 생성 (`components/apply/ApplicationSheet.tsx`, `lib/pdf/*`)

**그러면 지금 할 수 있는 게 뭐냐** → §3 의 **A 등급**이다. 겉으로 아무 변화가 없는 것.

---

## 2. 먼저 읽을 것 — 이 순서로

1. `docs/작업지시서.md` — **결정 로그. 현재 v1.03, 최신 항목 D-103.**
   각 D 번호에 「왜 그렇게 했나」와 **「무엇을 일부러 안 했나」**가 적혀 있다.
   🔴 **여기 적힌 판단을 뒤집는 리팩토링은 근거를 대야 한다.** 읽지 않고 뒤집지 마라.
2. `docs/3-할일/01-남은-일.md` — 맨 위 「지금 상태」와 ⓪(가장 급한 일)
3. `docs/2-학습/05-고칠-때-지켜야-할-것.md`
4. `docs/4-기록/10-디자인-규칙.md` — 색·그림자·글자·카드 규칙
5. `docs/4-기록/01-신청서-설계.md` — 신청서 구조와 「담당자가 만들 수 있는 것」의 경계

그다음 `git log --oneline -30`.
**첫 작업은 `git status` 이고 결과를 보고한다.**

---

## 3. 위험 등급 — 모든 후보를 셋 중 하나로 분류해라

| 등급 | 뜻 | 언제 |
|---|---|---|
| **A** | **아무도 알아챌 수 없다.** 화면·데이터·동작이 **완전히 동일**하고 개발자만 차이를 안다 | **지금 가능** |
| **B** | 화면이나 담당자 업무가 조금이라도 달라진다 | **접수 뒤**(10/7 이후) |
| **C** | 저장 데이터·규칙·시트 열·PDF 가 바뀐다 | **별도 승인 + 접수 뒤** |

**A 등급은 「동일함을 증명」해야 A다.** *"같을 것 같다"* 는 A가 아니다.
예: 인라인 유틸리티를 클래스로 바꿀 때는 **두 유틸리티 집합이 글자 그대로 같은지**
확인하고, 프로덕션 CSS(`.next/static/css/*.css`)에서 결과를 비교해라.
개발 모드는 즉석 생성이라 근거가 못 된다.

**A 등급의 예** — 죽은 코드 제거 · 타입 강화(`Record<모든값, …>` 로 빠짐을 컴파일
오류로 만들기) · 값이 동일한 중복 정의 합치기 · 주석·문서 정리 · 렌더 결과가 같은
클래스 치환.

**A 등급이 아닌 것** — 컴포넌트 쪼개기(렌더 트리가 바뀐다) · 여백·색 조정 ·
함수 이름 변경 중 화면 문구에 닿는 것 · 폴더 구조 변경.

---

## 4. 파트 분할 — 이 순서로 하나씩

**안전한 것부터**이다. 순서를 바꾸지 마라.

```
파트 1  타입과 도메인 정의       lib/types/index.ts · lib/ui/* · lib/forms/fields.ts
파트 2  죽은 코드와 안 쓰는 것   전역 — import·export·상수·클래스
파트 3  디자인 토큰과 공용 클래스 tailwind.config.ts · app/globals.css · components/ui/*
파트 4  공용 유틸               lib/firebase/*.ts 의 순수 함수 · 날짜·문자열 포맷
파트 5  담당자 화면             app/staff/**           ← 여기서부터 대개 B
파트 6  회원 화면               app/(홈·apply·mypage) · components/apply/**  ← B
파트 7  서버 동기화·PDF         lib/server/googleSync.ts · PDF 경로   ← C
```

**파트 1~4 안에도 B·C 가 섞여 있다.** 등급은 파트가 아니라 **변경마다** 매긴다.

### 파트마다 이 순서로

1. **읽는다** — 그 파트의 파일을 실제로 읽는다. 추측하지 마라
2. **보고한다** — 「지금 어떻게 되어 있나」 5~10줄 + **개선 후보 목록**
   (각 후보에 등급 A/B/C · 왜 · 무엇이 같이 흔들리나 · 확인 방법)
3. **사용자가 고른다** — 무엇을 할지, 지금 할지 미룰지
4. **하나씩 착수한다** — **한 커밋에 한 가지.** 여러 개를 묶지 마라
5. **확인 방법을 알려준다** — `npm run build` 로 잡히는 것인지, 사람이 화면을 봐야
   하는 것인지. 후자면 **무엇을 어디서(로컬/배포본) 보면 되는지** 구체적으로
6. **승인 뒤 커밋·푸시한다**

🔴 **1~2 를 건너뛰고 고치지 마라.** 후보 목록을 보여 주고 사용자가 고르게 하는 것이
이 세션의 핵심이다.

---

## 5. 이미 확인된 후보 — 출발점으로만. 직접 검증하고 등급을 매겨라

09-25 작업 중 실제로 확인한 것이다. **숫자는 그때 값이니 다시 세어라.**

**① 같은 원인의 사고가 하루에 세 번 났다 — 리팩토링의 진짜 주제**

- **화면마다 값을 짐작한다** → 설정이 하나 늘 때마다 그 수만큼 갈라진다.
  `groupEntry`(「신청은 누가 하나」)를 추가했을 때 상세만 고치고 목록 카드를 빠뜨려
  **같은 공고가 목록과 상세에서 다른 말을 했다**(D-101). `groupNoticeOf` 한 함수로
  묶어 해결했다. **같은 구조가 다른 곳에 또 있는지 찾아라.**
- **타입이 빠짐을 안 잡아 준다** → 새 필드를 더할 때 조용히 빠진다(D-101 의 `short`).
  `Record<모든값, …>` 로 강제되는 곳과 아닌 곳을 목록화해라. **대개 A 등급이다.**
- **다듬은 값을 편집 중인 값으로 쓴다** → `optionsOf` 가 `trim` 하는데 그걸 입력칸
  `value` 로 써서 **띄어쓰기를 칠 수 없었다**(D-100 결함 ①). 같은 패턴을 찾아라.

**② 흩어진 정의**
- `shadow-card` 를 **인라인으로 나열한 곳 42개**(`grep -rn "shadow-card" app components`).
  `.card` / `.card-raised` 클래스가 있는데도 쓰이지 않는다. **A 등급 후보이지만
  렌더 결과 동일을 증명해야 한다.**
- 그림자 정의가 **tailwind 토큰 4개 + `globals.css` 의 `.empty-card` 자체 box-shadow**
  로 세 곳에 흩어져 있다(D-103 별도 보고).
- `inputCls` 가 `app/staff/programs/page.tsx` 와 `components/staff/ProgramFieldRows.tsx`
  에 **의도적으로 복사**되어 있다(주석: "두 곳뿐이라 아직 묶지 않는다, 셋째가 생기면
  그때"). **셋째가 생겼는지 확인해라.**

**③ 큰 파일** (`wc -l`)
```
1283  lib/server/googleSync.ts
1195  app/staff/programs/page.tsx     ← 주석이 "1000줄 넘어 더 키우지 않는다"고 선언
1050  lib/types/index.ts
 741  components/apply/ApplicationForm.tsx
 624  app/staff/page.tsx
```
🔴 **줄 수만으로 쪼개자고 하지 마라.** `lib/types/index.ts` 는 「도메인 타입과 라벨의
단일 출처」라서 큰 것이 **목적**이다. 쪼개면 위 ①의 「갈라짐」이 더 늘어난다.
그리고 **컴포넌트 쪼개기는 A 등급이 아니다**(렌더 트리가 바뀐다).

**④ 인덱스가 박혀 있는 곳 — 접수 뒤, C 등급**
`lib/server/googleSync.ts` 의 `HEADERS` 는 17열, `APP_FIXED_WIDTHS` 는 인덱스,
`PDF_COL = 16`, `ensureHeaders` 는 `'A1:R1'` 로 박혀 있다. **열을 하나 더하면 네 군데가
동시에 흔들린다.**

---

## 6. 지켜야 할 세션 규칙

- **커밋·푸시는 네가 git 명령을 실행하되 반드시 사용자 승인 뒤에.** 커밋 메시지 끝에
  `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` 와 `Claude-Session: <이 세션 링크>`
- **새 문서 파일을 만들지 마라. 기존 문서에 써라.**
- 작업지시서에 기록할 때는 **D-번호 행 · 맨 위 판 번호 · 판 이력 표 셋 다** 갱신.
  다음 번호는 **D-104**. 🔵 **여러 파트를 한 D 번호로 묶지 말고 파트별로 나눠라.**
- `firestore.rules`·`storage.rules` 가 바뀌면 **알려라.** `npm run rules` 는 사용자가 직접 돌린다.
- **`.env.local` 내용과 키를 채팅에 올리지 마라.**
- **개발 서버를 띄우지 마라.** 시험이 필요하면 **무엇을 어디서(로컬/배포본) 보면 되는지**
  알려 주고 사용자가 직접 `npm run dev` 한다.
- **파일 이름·함수 이름을 쓸 때는 「그게 무엇이고 화면 어디에서 보이는지」를 같이 써라.**
  사용자는 코드를 읽지 않는다. `lib/config/venues.ts` 의 `Venue.notice` 라고만 쓰면 통하지 않는다.
- **무조건 찬성하지 마라.** 사용자 판단이 틀렸다고 보면 근거를 대고 반대해라.
  09-25 에 사용자의 가설 하나가 코드 확인 결과 뒤집혔고, 반대로 내가 지어낸 안내
  문구가 사실이 아니어서 빠졌다. **양쪽 다 있었다.**

---

## 7. 하지 말 것

- **금지선(§1)을 넘는 변경.** 애매하면 하지 않고 물어본다
- `prettier`·`eslint --fix` 통째로 돌리기 — 진짜 변경이 서식 변경에 묻힌다
- 「폴더 구조를 다시 짜자」 같은 한 덩어리 변경. **되돌릴 수 없는 것은 하지 않는다**
- 한 커밋에 여러 가지 섞기
- 동작 변경을 리팩토링이라고 부르기
- 작업지시서를 **읽지 않고** 기존 판단 뒤집기
- 며칠 걸리는 작업 시작하기 (§0)
- 테스트 프레임워크 도입을 슬쩍 끼워 넣기 (지금 없다. 필요하다고 보면 **별도 후보**로
  등급과 근거를 달아 제안하고, 승인 없이 시작하지 마라)

---

## 8. 첫 답변에 담을 것

1. `git status` 결과 — **다른 세션의 미커밋 변경이 있는지** 포함
2. §2 문서를 읽고 파악한 **현재 상태 5줄 요약**
3. **§1 금지선을 네 말로 한 줄 요약** (이해했는지 확인용)
4. **파트 1(타입과 도메인 정의) 분석 보고 + 개선 후보 목록**
   — 각 후보에 등급 A/B/C, 왜, 확인 방법

그리고 멈춰서 사용자가 고르기를 기다린다. **바로 고치기 시작하지 마라.**
````

---

## 3. 언제 이 세션을 열면 좋은가

### ✅ 여는 것이 좋은 때

| 상황 | 왜 |
|---|---|
| **급한 일이 없는 날** | ②는 언제든 접을 수 있어야 합니다. 급한 일 사이에 끼워 넣으면 중간 상태로 남습니다 |
| **접수가 막 끝난 직후** | B 등급(화면이 달라지는 것)을 할 수 있는 유일한 창입니다 |
| **같은 종류의 사고가 두 번 났을 때** | 그건 코드 구조가 틀린 신호입니다. ②에게 원인을 찾게 합니다 |
| **새 기능을 붙이기 직전** | 붙일 자리가 지저분하면 먼저 정리하는 편이 빠릅니다 |

### 🔴 열지 말아야 할 때

| 상황 | 왜 |
|---|---|
| **접수 마감이 며칠 안** | 회원이 신청서를 고치는 중입니다. 지금은 아무것도 만지지 않는 게 최선입니다 |
| **구현 세션이 돌아가는 중에 같은 파일** | 같은 폴더라 충돌합니다. 파트를 다르게 주거나 기다립니다 |
| **교수님께 보여 줄 것이 급할 때** | ②는 겉모습이 안 바뀌는 작업입니다. 보여 줄 게 안 나옵니다 |
| **내가 결과를 확인할 시간이 없을 때** | ②는 단계마다 제 승인이 필요합니다. 승인 없이 쌓이면 되돌리기 어려워집니다 |

---

## 4. 어떻게 물어보면 좋은가

### 파트 보고를 받은 뒤 — 이렇게 고릅니다

에이전트가 후보 목록을 주면 **전부 하라고 하지 마세요.** 다음 셋 중 하나로 답합니다.

```
「A 등급만 하고, B·C 는 남은-일에 적어 두세요」        ← 접수 중에 기본값
「①번만 먼저 하고 결과를 보여 주세요」                ← 처음 몇 번은 이렇게
「③번은 왜 지금 해야 하는지 다시 설명해 주세요」      ← 근거가 약할 때
```

### 잘 듣는 질문 — 실제로 효과가 있었던 것들

| 물어보기 | 왜 효과가 있나 |
|---|---|
| **「당신의 사고과정을 단계별로 설명해 주세요」** | 결론만 받으면 틀린 전제를 못 봅니다. 실제로 이 질문에서 잘못된 전제가 두 번 드러났습니다 |
| **「그게 무엇이고 화면 어디에서 보이는지 알려주세요」** | 파일 이름만 받으면 확인할 수가 없습니다 |
| **「이걸 안 하면 무엇이 고장나나요」** | *"깔끔해진다"* 만 나오면 지금 할 일이 아닙니다 |
| **「되돌리려면 어떻게 하나요」** | 되돌릴 방법이 없는 변경은 받지 않습니다 |
| **「내가 무엇을 어디서 확인하면 되나요」** | 로컬인지 배포본인지, 무엇을 눌러야 하는지를 받아야 검수가 됩니다 |
| **「이게 규칙 파일을 바꾸나요」** | 바뀌면 제가 `npm run rules` 를 직접 돌려야 합니다 |

### 잘 안 듣는 질문

| 이렇게 묻지 마세요 | 무슨 일이 생기나 |
|---|---|
| 「코드 좀 깔끔하게 정리해줘」 | 범위가 없어서 한 번에 수십 파일을 건드립니다 |
| 「제일 좋은 방법으로 해줘」 | 판단을 떠넘긴 것이라, 나중에 왜 그랬는지 아무도 모릅니다 |
| 「이거 괜찮아?」 | 대개 「괜찮다」가 돌아옵니다. **무엇이 틀릴 수 있는지**를 물어야 합니다 |
| 「알아서 다 해줘」 | ②에게는 특히 위험합니다. 승인 단계가 사라집니다 |

---

## 5. 답을 어떻게 믿나 — 에이전트가 틀리는 신호

**에이전트는 그럴듯하게 틀립니다.** 아래 신호가 보이면 한 번 더 캐물으세요.

| 신호 | 09-25 에 실제로 있었던 일 |
|---|---|
| **파일을 읽지 않고 단정한다** | *"대비가 부족하다"* 는 디자인 진단이 **틀렸습니다.** 대비는 이미 있었고 줄글로 엉켜서 안 보인 것이었습니다 |
| **원본에 없는 이유를 만든다** | 「팀 대표자」 칸에 *"대표자에게 우선 연락드립니다"* 를 붙였는데 **신청서에 없는 내용**이었고 사실도 아니었습니다 |
| **고쳤다는데 반쪽만 고쳤다** | 카드 문구를 고쳤다면서 **전용 양식이 덮어쓰는 경우를 빠뜨려** 해커톤 카드는 그대로였습니다 |
| **시험 항목이 실제와 다르다** | *"제출 실패 때 그 칸으로 이동한다"* 고 시험을 요구했는데 **그 기능은 원래 없었습니다** |
| 「아마」 「~일 것입니다」 | 재거나 읽어서 확인할 수 있는 것을 짐작한 것입니다. **재라고 하세요** |

**반대로, 제 판단이 틀린 적도 있습니다.** 「동의 항목을 지우고 새로 만들면 되겠다」는
가설이 코드 확인 결과 **더 나쁜 방법**이었습니다(지우면 기록이 두 갈래로 쪼개집니다).
그래서 프롬프트에 **「무조건 찬성하지 마라」** 를 넣어 둡니다.

### 믿을 수 있는 답의 모습

- **파일과 줄 번호**가 붙어 있다 (`ProgramCard.tsx:136`)
- **숫자를 쟀다** — *"375px 에서 카드 안쪽 301px, 이 줄은 244px"*
- **무엇을 안 했는지 말한다** — *"단일 시점 호출은 건드리지 않았습니다"*
- **자기 실수를 먼저 말한다** — *"제 시험 안내가 틀렸습니다"*

---

## 6. 두 세션을 같이 돌릴 때 — 지켜야 할 것

같은 폴더(`D:\work\iLINE-edu-support`)를 두 세션이 씁니다. 파일이 섞이면 **어느 쪽이
무엇을 고쳤는지 알 수 없게** 됩니다.

- 🔴 **각 세션의 첫 작업은 `git status`.** 남의 미커밋 변경이 있으면 그 파일은 피합니다
- 🔴 **한 단계 끝나면 커밋.** 커밋하지 않은 변경을 남긴 채 세션을 떠나지 않습니다
- **파트를 겹치지 않게 줍니다.** 구현 세션이 신청서를 만지는 동안 ②에게
  신청서 파트를 주지 마세요
- 새 브랜치를 만들지 않습니다. `main` 에 작은 커밋으로 쌓습니다

---

## 7. 관련 문서

| 궁금한 것 | 볼 문서 |
|---|---|
| 에이전트가 말한 파일 이름이 무엇인가 | `docs\claude 생성 파일 설명서.md` |
| 코드를 고칠 때의 규칙 | `docs\2-학습\05-고칠-때-지켜야-할-것.md` |
| 왜 그렇게 정했나 (D-번호) | `docs\작업지시서.md` |
| 아직 안 한 일 | `docs\3-할일\01-남은-일.md` |
| 색·그림자·글자 규칙 | `docs\4-기록\10-디자인-규칙.md` |
