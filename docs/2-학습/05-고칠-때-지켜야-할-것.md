# 고칠 때 지켜야 할 것

> 사이트 코드를 건드릴 때 **매번 확인하는 규칙**입니다.
> 여기 있는 항목은 전부 **실제로 사고가 났던 것**들이라, 짐작으로 쓴 규칙이
> 하나도 없습니다. 날짜는 그 사고가 난 날입니다.
>
> 기준일: 2026-09-08

---

## 📌 되풀이하지 말 것 (09-07에 값을 치르고 배운 것)

**① 상태를 새로 만들면, 그 상태를 읽는 곳을 전부 찾아 고친다.**
09-05에 `MemberGate` 의 '조회 중' 문제를 고치며 `loading` 상태를 만들었는데
**헤더는 그 구분을 쓰지 않아** 같은 버그가 그대로 남아 있었습니다.
새 상태를 추가할 때는 그것을 읽는 파일을 **전부 검색**하세요.

**② 리액트 상태 변수로 "지금 로그인한 사람"을 판단하지 않는다.**
그 값은 함수를 붙잡은 쪽 기준으로 **한 박자 낡을 수 있습니다.**
방금 바뀐 값이 필요하면 `getAuthClient().currentUser` 처럼 **출처에 직접
묻습니다.** (R-1의 진짜 원인)

**③ 시험 결과가 이상하면, 그 화면이 어느 버전을 돌고 있는지부터 확인한다.**
"PC는 되는데 휴대폰은 안 된다"의 정체가 기기 차이가 아니라 **push 전후
차이**였습니다. 로컬은 새 코드, 배포본은 옛 코드였습니다.

**④ 화면을 하나 고치면 같은 성격의 화면을 나란히 놓고 본다.**
회원가입 화면에는 있던 처리가 **로그인 화면에만** 빠져 있었습니다 (R-3).

**⑤ 검사 순서를 브라우저에 맡기지 않는다.** (09-08)
입력칸에 `required` 만 붙여 두면 **브라우저가 자기 순서로** 먼저 지적합니다.
체크박스·라디오·첨부는 `required` 로 표현되지 않아서, 화면 위쪽의 체크박스를
비워 뒀는데 **아래쪽 '팀명'을 먼저 지적**하는 일이 실제로 있었습니다.
신청자는 위에 문제가 있는 줄 모르고 아래만 채웁니다.
→ 폼에 **`noValidate`** 를 붙이고 **검사 순서를 우리가 화면 순서대로** 씁니다.
`required` 속성은 별표(*)와 스크린리더 안내를 위해 그대로 둡니다.

**⑥ 신청서 원본(PDF)에 칸을 새로 넣으면 `data-pdf-keep` 을 함께 단다.** (09-08)
PDF는 화면을 **한 장의 긴 그림으로 찍어** A4 크기로 자릅니다. 그냥 자르면
**표 한 줄이 두 장에 걸쳐 반토막**이 납니다. 표의 각 줄·목록의 각 항목처럼
"여기서 잘리면 안 되는" 덩어리에 `data-pdf-keep` 을 달면, 그 경계에서만
페이지가 넘어갑니다 (`lib/pdf/applicationPdf.ts`).

**⑦ 저장소 안에 소스 파일 사본을 두지 않는다.** (09-08 · Vercel 빌드 실패)
`Claude outputs/` 폴더에 내려받아 둔 `ApplicationForm.tsx` 사본이
**빌드를 깨뜨렸습니다.** 쓰지도 않는 파일인데, 타입 검사는 **저장소 안의
모든 `.tsx`** 를 보기 때문입니다. 그 사본은 옆에 없는 `./PortraitConsent`
를 불러오고 있었습니다.
· 내려받는 자리는 **저장소 밖**에 둡니다 (예: `D:\work\Claude outputs`)
· 저장소 안에 둘 수밖에 없으면 `.gitignore` 와 `tsconfig.json` 의
  `exclude` **양쪽에** 넣습니다. 한쪽만으로는 막히지 않습니다

**⑧ "내 쪽에서는 빌드가 됐다"는 것만으로는 근거가 못 된다.** (09-08)
위 사고 때 제 쪽 빌드는 멀쩡히 통과했습니다. **그 폴더가 제 쪽에는 없었기
때문**입니다. 배포만 깨지면 코드부터 의심할 게 아니라 **양쪽 파일 목록을
먼저 맞춰 봅니다.** 목록이 같고 내용도 같은데 배포만 깨진다면, 그때는
저장소에 있는데 **양쪽 어디에도 안 보이던 것**을 찾습니다.

**⑨ 결정이 바뀌면 「이용자가 읽는 문구」를 전부 훑는다.** (09-10)
D-29(임시저장 없음) · D-40(신분증 안 받음) · D-46(사유 비공개) · D-47(갤러리
내림)을 정하고도 **FAQ·홈 화면·사업소개·검색 설명**이 옛 내용 그대로였습니다.
처리방침에서 신분증 문구를 고쳤을 때 **같이 훑었어야** 했는데 그 화면만 봤습니다.

**이용자는 화면에 적힌 것을 그대로 믿습니다.** "임시저장됩니다"를 읽고 창을
닫으면 글이 사라지고, "신분증을 제출하라"를 읽으면 없는 서류를 준비합니다.
→ 결정을 바꾸면 **아래를 한 번에 검색**하세요.
```
app/  components/  에서  기능 이름으로 grep
예) 임시저장 · 신분증 · 통장 · 갤러리 · 검토 중 · 사유
```
· 화면 본문뿐 아니라 **FAQ · 홈 단계 안내 · 사업소개 · `layout.tsx` 의
  검색 설명 · 약관**까지 봅니다

---

## ⚠️ 어디서 시험할 것인가 — 로컬 vs 배포본

### 먼저 알아야 할 것: **데이터베이스는 하나입니다**

로컬(`localhost:3000`)이든 배포본이든 **같은 Firebase 프로젝트**를 봅니다.

```
localhost:3000  ─┐
                 ├─→  Firebase (iline-edu-support)  ← 하나뿐
배포 주소       ─┘        회원 · 신청서 · 공지 · 파일
```

**즉 로컬에서 한 시험도 진짜 데이터를 만듭니다.** 로컬에서 가입한 계정, 올린
공지, 제출한 신청서가 **배포된 사이트에도 그대로 보입니다.** "로컬이니까
지워도 되겠지"가 아니라, **로컬 시험도 정리 대상**입니다.

반대로 **보안 규칙을 콘솔에 게시하면 로컬에도 즉시 적용**됩니다. 규칙 시험은
로컬에서 해도 결과가 같습니다.

---

### 기준표

| 무엇을 고쳤나 | 어디서 시험 | 왜 |
|---|---|---|
| 화면·폼·흐름 (버튼, 안내 문구, 유효성 검사) | **로컬이면 충분** | 고치고 바로 확인. 배포 기다릴 이유 없음 |
| 보안 규칙 | **로컬이면 충분** | 규칙은 서버에 하나뿐이라 결과가 같음 |
| **서버 코드** (`lib/server/`, `app/api/`) | **배포본 필수** ⚠️ | 아래 참고 |
| 구글 시트·드라이브 연동 | **배포본에서만 동작** | 연동 값 5개가 Vercel 에만 있음. 로컬은 조용히 건너뜀 |
| **날짜·시각 표시** | **배포본 필수** ⚠️ | 아래 참고 |
| 휴대폰 화면 (D-24) | **배포본** | 실기기가 `localhost` 를 못 봄 |
| 도메인·인증서 | **배포본** | 로컬에 해당 없음 |

### ⚠️ 로컬에서는 절대 안 잡히는 것 두 가지

**① 시간대** — 로컬 PC는 **한국 시간**, Vercel 서버는 **UTC** 입니다.
서버에서 만드는 값(드라이브 파일명, 시트의 신청 일시)은 **로컬에서 시험하면
언제나 맞게 나옵니다.** 09-06 의 9시간 오차가 정확히 이 경우였습니다.

**② 환경변수** — 서비스 계정 키·시트 ID 등은 Vercel 에만 있습니다.
로컬에서는 연동이 **오류 없이 건너뜁니다**(의도한 동작). 그래서 로컬에서
"잘 되네" 하고 넘어가면 배포본에서 처음 실패합니다.

> **한 줄 규칙**: 브라우저 안에서 끝나는 일은 로컬, **서버가 관여하면 배포본.**

---

## ⚠️ 고친 뒤 반영하는 순서

**Vercel 은 GitHub 에 있는 코드로 배포합니다.** 로컬에서 고친 것은 push 해야 올라갑니다.

1. 로컬에서 확인
2. `git add . && git commit -m "..." && git push`
3. Vercel 자동 재배포 (1~2분)
4. **배포된 주소에서 다시 확인** — 위 표의 '배포본 필수' 항목이 있으면 반드시

*(09-05: push 를 빠뜨려 배포본에만 옛 코드가 남아 신청서 제출이 실패한 적 있음)*

**보안 규칙은 GitHub 과 무관합니다.** 콘솔에 따로 게시해야 하고 push 로는
반영되지 않습니다. **규칙과 코드를 함께 고쳤다면 규칙을 먼저** 올리세요 —
반대로 하면 새 코드가 옛 규칙에 막혀 전부 실패합니다.

> 콘솔에서 `게시` 버튼이 흐리면 **내용이 이미 같다는 뜻**입니다. 정말 반영됐는지는
> 동작으로 확인하세요 (예: 담당자 계정으로 공지 작성이 되는지).

---
