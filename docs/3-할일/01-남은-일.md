# 남은 일 — 언제 해야 하는가 순서로

> **끝난 것은 `docs/4-기록/05-완료-이력.md` 로 옮겼습니다.** 여기에는
> **아직 안 한 것만** 남아 있습니다.
> 답을 기다리는 항목은 `02-답-기다리는-것.md` 에 따로 모았습니다.
>
> 최종 정리: 2026-09-08

---

## 📍 지금 상태 한 줄

**🎉 2026-09-18 접수 개시.** aiedu.iline.or.kr 단독으로 열었고 AI-EDU 연구반(`2026-ai-edu`, 마감 9/21 18:00)이
접수 중입니다. 인트로 허브 머지는 보류. 정리 스크립트는 저장소에서 뺌. Vercel 은 Hobby 로 개시(한 장 ⑤).

> **09-21 인수인계 — 이 뒤로는 Code 모드 세션이 이어받습니다.** Cowork 세션은 PC 에 파일을 덮어쓰는
> 방식이라 **미커밋 변경이 남아 있을 수 있음 → 첫 작업은 `git status`.** 전체 맥락은 프로젝트 지식
> `claude/working-context.md`(09-21 판), 결정 로그는 `docs/작업지시서.md` v0.85 · D-93. 아래 1~8 이 지금 순서.
> 09-21 Code 세션에서 1~3 끝냄(커밋 dca80dc · 규칙 배포 · 배포본 시험 · 담당자 계정), 이어서 **5 끝냄**(D-94).
> 09-22: **D-95~D-97** (단체 안내·해커톤 양식·상태 배지 색). 바로 할 것은 아래 ⓪.

**⓪ 🔴 9/27(일) — 데이터 디깅 3명이 새 양식으로 다시 저장했는지 확인**

✅ **09-25 에 끝낸 것** — 양식 최종(칸 5개: 팀명[팀명 칸 지정] · 팀에서의 역할 · 팀 구성 요건
확인 · 수료 기준 확인 · **결과물 오픈소스 공개 동의[동의·확인 필수]**) · 신청 화면 확인 ·
4명 연락(2명 iSERI 직접, 2명 운영 쪽) · 교수님 답 두 개(⑪-1 팀 없는 사람 안 받음,
⑪-2 오픈소스 동의해야 참가).

**지금 상태** — **1명**은 새 양식으로 다시 저장(팀명 입력 · 오픈소스 「동의함」),
**3명**은 아직.

🔴 **왜 9/27 인가** — 마감이 **9/30(수) 18:00** 이고, **마감 뒤에는 본인이 못 고칩니다.**
그러면 담당자가 전화로 받아 적어야 합니다. 9/27 에 보면 안 한 사람에게 연락하고도
**사흘이 남습니다.**

- [ ] **누가 다시 저장했나** — 구글 시트 첫 탭, 데이터 디깅 4줄의 **「프로그램별 기재」** 열.
      **`팀명:` 줄이 있으면 새 양식으로 저장한 것**, 없으면 아직입니다.
      (「수정」 열에 `1회 · 날짜` 가 찍혀 있는지로도 볼 수 있습니다)
- [ ] **저장한 사람 → 팀명 짝 맞추기** — 두 사람씩 팀명이 **띄어쓰기까지 똑같은지.**
      하나만 달라도 다른 팀으로 묶입니다. 어긋나면 그 사람에게 연락
- [ ] **아직인 사람 → 다시 연락** — 운영 매뉴얼 **§3-6 ⓐ** 문구 · **9/30(수) 18:00** 명시
- [ ] **마감 전에 한 번 더** — 9/27 에 연락한 사람이 저장했는지 **9/30 18시 전에**

⚠️ **먼저 저장한 1명의 줄은 다르게 보이는 게 정상입니다.** 그 학생은 오픈소스 칸을
「고르기」에서 「동의·확인」으로 바꾸기 **전에** 저장해서, 시트에 **`결과물 오픈소스 공개: 동의함`**
(옛 칸)이 남아 있습니다. 나머지 3명은 **`결과물 오픈소스 공개 동의: 확인함`**(새 칸)으로
나옵니다. 둘 다 동의한 것이라 **연락할 필요 없습니다.**

⚠️ **새로 온 2명의 팀 상황은 아직 모릅니다**(먼저 온 2명은 같은 팀 — 09-24 iSERI).
팀명이 들어오면 저절로 드러납니다.

**⓪′ ✅ 해결 (D-105 · 09-25 배포 · 데이터 디깅 체크 완료)**

「칸 추가」로 만든 팀명이 산출물에서 안 묶이던 것(D-104 F1)을 **D-105 로 고쳤습니다.**
담당자가 글 상자에 **「이 칸이 팀명입니다」** 를 체크하면 그 칸의 답으로 팀을 묶습니다.
칸 이름으로 짐작하지 않습니다 — 이름을 「팀 이름」으로 바꿔도 안 깨집니다.

- [x] 🔴 **데이터 디깅 공고에 체크하기** — ✅ 09-25 배포 뒤 iSERI 완료 — `/staff/programs` → 데이터 디깅 → 수정하기 →
      「팀명」 글 상자 줄의 **「이 칸이 팀명입니다」** 체크 → 저장
      · **이미 받은 팀명은 그대로 남습니다.** 답은 칸의 이름표(`fid`)에 묶여 있고
        체크는 그 이름표를 바꾸지 않습니다. 4명이 다시 적을 필요 **없습니다**
      · 체크해도 **동의·고르기 답이 풀리지 않습니다**(그건 본문·선택지가 바뀔 때만)
      · 🔴 **산출물 제출이 열리기 전에.** 산출물을 내는 순간 팀명이 복사되어 굳습니다
- [ ] 체크 뒤 `/staff` 에서 **팀명 짝이 맞는지** 확인 (⓪ 의 셋째 칸과 같은 일)

🔵 **해커톤·AI-EDU 는 할 일 없습니다** — 전용 양식이 팀명을 따로 받습니다.

**① 📋 지금 순서 — 추가 요청 다섯 가지 (09-25 iSERI 와 정함)**

| 순서 | 무엇 | 분량 | 상태 |
|---|---|---|---|
| 1 | **새 단체 공고에 팀명 칸 미리 넣기** | 1~2시간 | ✅ D-107 (09-25) |
| 2 | **정산 계좌 칸 없애기** + 처리방침 개정 준비 | 반나절 | ▶ 다음. **첫 정산이 들어오기 전에** — 그러면 계좌를 한 건도 받지 않은 채로 끝난다 |
| 3 | **공지사항 첨부파일** | 반나절~하루 | 규칙 변경 없음(공개 저장소 `support/public/` 이미 있음) |
| 4 | **담당자가 회원 정보 보기** | 반나절~하루 | 💬 화면 구성 의논 먼저. 규칙 변경 없음(담당자는 이미 회원 목록을 읽을 수 있다) · 시트로 내보내지 않음 |
| 5 | **정산 서류 두 종류 · 프로그램마다 다르게** | 하루 | 💬 설계 의논 + ⏳ 회의록 형식 답(`02` B-8) |
| 6 | **시설 예약 이틀 전까지** | 하루 | ⏳ 운영진 답(`02` B-7). 설정값이 아니라 **주간 전달 구조를 바꾸는 일** |

- ✅ **서식 자료실 탭 숨김** (D-106 · 09-25) — 늘 빈 화면이었다. 살릴지는 한 장 ⑧.
  살리면 3번의 파일 올리기 부품을 그대로 쓴다
- 정산을 둘로 나눈 이유 — 계좌 없애기는 막힘이 없고, 「프로그램마다 다른 서류」는
  공고 화면에 정산 서류 설정을 새로 만드는 일이라 의논이 필요하다
- 처리방침에 계좌번호가 수집 항목으로 **네 군데** 있다. 수집은 바로 멈춰도 되고(적힌 것보다
  덜 받는 건 문제없다), 문구는 **시행 7일 전 공지** 뒤에 고친다

> 그다음: **9/26 교수님 면담**(한 장 ⑪-1 팀 편성 방식이 가장 급함) → 요청 8(공지 첨부).
> 해커톤·데이터 디깅 공고는 **둘 다 올라가 접수 중**입니다.

**교수님 추가 요청 (09-18 개시 뒤)** — 순서대로

1. [x] **AI-EDU 접수 마감을 9월 21일 18:00 으로** — ✅ 09-21 확인 — 담당자 화면 → 프로그램 관리 → 「수정」 → 접수 마감.
      코드 변경 없음. 바꾼 뒤 목록 카드의 D-day 와 상세 화면 마감 표시 확인
2. [x] **관리자 계정 하나 더** — ✅ 09-21 교수님이 준 이메일로 `grant-staff.mjs --env` 실행, 「관리」 확인.
      테스트 계정(`test…@anver.com`)은 정리 권고
3. [x] **1:1 문의** — ✅ 09-18 구현 (D-93). 비공개·회원만·시트 「문의」 탭·마이페이지 새 답변.
      ✅ 09-21 규칙 배포(`npm run rules`) · 배포본 시험 통과(문의 → 시트 줄 → 배지 → 답변 → 새 답변 → 종료 → 회색 줄)

**교수님 추가 요청 (09-21 연락)** — 확인 결과와 순서

4. [ ] 🆕 **새 프로그램 공고 — 두 건**

   **4-A. 🔴 데이터 디깅 캠프 — 공고는 이미 올라가 접수 중입니다 (마감 9/30 18:00)**
      배포본에 `2026-data-digging` 으로 등록돼 있습니다. **두 가지를 고쳐야 합니다.**

      ① **참여 방식이 「개인 신청」으로 되어 있습니다** → 화면이 *"개인별 신청 (팀 활동이어도
         각자)"* 라고 말합니다. **「단체 프로그램」 + 「팀원이 각자 신청」** 으로 바꾸세요
         (D-99′ 로 그 선택이 생겼습니다). 그러면 *"팀원 모두가 따로 냅니다 · 똑같은 팀명을
         적어야 묶입니다"* 안내가 나갑니다
      ② **신청서에 칸이 없습니다** — 지금은 글칸 하나 + 첨부 하나뿐이라 팀명도 동의도 못 받습니다.
         D-99 를 배포한 뒤 「칸 추가」로 **네 개**만 만들면 됩니다:

      | # | 칸 | 이름 | 필수 |
      |---|---|---|---|
      | 1 | 동의 | 팀 구성 요건 확인 | ✅ |
      | 2 | 동의 | 수료 기준 확인 | ✅ |
      | 3 | 글 상자(한 줄) | 팀명 | ✅ |
      | 4 | 동의 | 활동 결과물 오픈소스 공개 | 결정 |

      · **팀원 2 정보 칸은 만들지 마세요** — 각자 신청하므로 각자 자기 정보를 냅니다
      · **개인정보 동의·초상권 동의도 만들지 마세요** — 사이트가 이미 받습니다(D-96′·D-44)
      · 🔴 **이미 들어온 신청이 있으면** 칸을 더해도 그 건에는 답이 없습니다. 건수를 먼저 확인하세요
      · 🔴 **신청서 원본(PDF)은 「대표 1인이 신청」** 이라고 되어 있습니다. 각자 신청으로 가면
        **공고 소개글과 교수님 쪽 안내도 같이 고쳐야** 학생이 안 헷갈립니다

   **4-B. 해커톤** — 전용 양식은 **이미 만들어 두었습니다**(D-96 · 담당자 「신청 양식」 드롭다운에
      「AI-EDU Next Class 해커톤 (2026)」). 🔴 접수 시작일·활동 일정·지정 템플릿 파일·참가 유의사항
      문구를 받아야 공고를 엽니다. 마감은 2026-10-06 24:00
5. [x] 🆕 **2316호 화요일 18:30~21:00 외부 사용 표시** — ✅ 09-21 (D-94). 예약 시간(09~18)과 안 겹쳐
      막을 것은 없고 **안내만**. `Venue.notice` 새 필드에 문구 한 곳 → **예약 화면**(📍 줄 아래)과
      **담당자 「직접 예약 추가」** 두 곳이 같은 문자열을 씀. 담당자 화면을 추가한 이유: 거기만 0~24시를
      다 열어 둬서 저녁 조율 건을 넣을 때 실제로 겹칠 수 있음. 「학기 중」 한정어 없음 · **FAQ 제외**
      (둘 다 iSERI 결정). 사정이 바뀌면 `lib/config/venues.ts` 의 그 한 줄만 고치면 됨
6. [x] **산출물 여러 건** — ✅ 이미 됨. 한 사람(팀)이 「새로 제출하기」를 여러 번 할 수 있고(1:N 설계,
      D-76), 한 건에 파일 최대 10개. 교수님께 "됩니다" 로 답하면 됨
7. [x] **정산 파일 여러 개** — ✅ 이미 됨. 영수증 한 번에 여러 장 선택(`multiple`), 최대 10장 · 1장 20MB.
      다시 제출하면 새 파일이 추가됨. 교수님께 "됩니다" 로 답하면 됨
8. [ ] 🆕 **공지사항 첨부파일** — 지금은 제목·내용만(`Notice` 에 파일 칸 없음). 만들 것: 담당자 공지
      작성에 파일 첨부(여러 개, 공개 Storage `support/public/notices/{id}/`), 공지 상세에 다운로드 목록,
      storage.rules 공개 읽기·담당자 쓰기(포스터와 같은 경로 규칙이면 규칙 변경 없을 수도). 🔴 허용
      형식·크기 결정(권장: PDF·HWP·DOCX·XLSX·이미지 · 20MB). 서식 자료실(`/notice/resources`)이 이미
      "파일 배포" 자리인데 비어 있음 — 공지 첨부와 자료실 중 무엇을 쓸지 정리 필요
      · ✅ 09-25 정리 — **자료실 탭은 숨김(D-106)**, 공지 첨부를 먼저 만든다(위 ① 의 3번).
        공개 저장소 규칙이 이미 있어 **규칙 변경 없음**. 자료실을 살리면 같은 부품을 쓴다



**신청자·담당자 전 구간이 배포본에서 동작합니다.** (09-12 밤: 시설 예약 ·
정산 지급 완료 · 영수증 드라이브 연동까지 배포본 확인 완료)
접수 개시를 막는 **기술 항목은 없고**, 남은 것은 **교수님 답 · 가정값 · 콘텐츠**입니다.

**다음에 앉으면 (09-17~)**
0. ✅ **시설 예약 해소 (09-17)** — 행정실 전달은 iSERI 가 직접 처리, 운영 시간
   09~18 확인, 현장 번호 없음, 2334 정원 불필요. **예약은 개시를 막지 않습니다.**
1. ✅ **처리방침 09-18 게시 (D-77)** — 시행일 2026. 9. 18., 「임시 문안」 뗌. 교수님
   답·법무 검토는 **개정 공지**로. → **개시를 막는 것이 없습니다.**
   `1-운영/07-접수-개시-체크리스트.md` §1 시험 데이터 정리 → §5 공개 순서로 — 모든 것에 앞선다
2. 안 왔으면 **09-14 미팅 후속(아래 🆕 M 절)** 순서대로 —
   ⓐ ~~디자인 규칙 문서 · 글꼴~~ ✅ → ⓑ ~~홈 재구성~~ ✅ → ⓒ ~~신청서 마감 전 수정~~ ✅ 09-16 →
   ⓓ 처리방침 초상권 절(숫자는 답 오면) → ⓔ 산출물·사진 제출(③ 답 오면) → ⓕ 디자인 방향(④ 답 + 브레인스토밍)
3. 접수 개시 직전: 체크리스트 §0 표 갱신 → 정리 스크립트 전체 청소 → 스크립트 삭제

```
① 접수 개시 전에 반드시    **없음** — 처리방침 09-18 게시 ✅ · 사업소개 ✅ · 예약 ✅ → 체크리스트 §1·§5
② 접수 개시와 함께         (공고 없이 개시 — 09-13. 다음 프로그램 때 공고 등록)
③ 접수 개시 전에 반드시    ~~시설 예약 구현~~ ✅ (운영값만 대기)
④ 접수 개시 이후           정산 잔여 · 인트로 허브 병합
M 09-14 미팅 후속          홈 재구성 · 신청서 수정 · 처리방침 초상권 · 산출물 제출 · 디자인 (아래)
```

⚠️ **최우선은 창의재단 사이트 접수 개시**입니다. 09-13부터 "개시"의 뜻이
바뀌었습니다 — **공고 없이 사이트를 열어 회원가입·시설 예약부터 쓰게 하는 것**이
개시입니다.

🔄 **09-17 정정 (D-75)** — 개시를 막는 것은 **처리방침 문안(A-4) 하나뿐**입니다.
같은 날 iSERI 답으로 예약 쪽은 전부 해소됐습니다(행정실 전달은 직접 처리 · 운영
09~18 확인 · 현장 번호 없음 · 2334 정원 불필요). 09-10(D-56)에는 예약 가정값
전체를 개시 조건으로 묶었는데 iSERI 지적대로 과했습니다 — 취소 시한·문의 창구·
범위(주)·요일은 설정 화면에서 **개시 후 언제든** 바꿔도 기존 예약자가 손해 보지
않습니다. 운영 시간만 "건물에 대한 사실"이라 다르지만, 그건 교수님이 아니라
**행정실 통화 한 번**이고 좁게 잡으면 위험이 없습니다. 합의만은 값이 아니라
**약속**이라 나중에 고칠 수 없습니다 — 없으면 회원이 보는 「확정됨」이 거짓말이 됩니다.

---

# 🆕 M. 09-14 교수님 미팅 후속 (D-71)

> 근거: `현황정리.txt`(iSERI 메모) → 09-15 2차 검증 → iSERI 결정. 교수님이 **명시**한
> 것만 확정으로 잡았고, 범위·내용이 메모에 없던 것은 iSERI 가 09-15에 정했다.
> 교수님께 남은 질문은 `03-교수님께-여쭐-것-한장.md` ③④.

**확정 — 순서대로**
- [x] **M-0 디자인 규칙 문서 + 글꼴 탑재** (09-15) — 어떤 답이 와도 안 바뀌는 기반.
      `4-기록/10-디자인-규칙.md` · Pretendard 동적 서브셋(npm `pretendard`, `app/layout.tsx` import).
      색은 **보류** — 교수님이 "iLINE과 꼭 통일할 필요 없다" → 브레인스토밍 뒤 토큰만 교체
- [x] **M-1 홈 재구성** ✅ 09-15 (D-72) — 첫 화면 = 「지금 접수 중인 프로그램」 남색 띠,
      **접수 중인 것만** 최대 3개(1개 넓게 / 2·3개 칸 / 4개↑ 3개 + 전체 보기), 없으면
      "없습니다" + [지난 프로그램 보기]. 참여 절차·사업명 히어로 삭제. `Button`·`SectionTitle`
      신설, 프로그램에 **활동 시작·종료일**(선택) 추가, 설명 글자색 한 단계 진하게.
      `components/home/OpenPrograms.tsx`(옛 CurrentProgramCard 대체) · `app/page.tsx`
      · 🤔 **고민거리** — 휴대폰에서도 개수를 제한할지(지금은 3개가 세로로 쌓임 ≈ 화면 2.5장).
        "더 보기"로 접거나 1개 + 나머지는 제목만, 같은 안. 실제 공고 2개 이상 올릴 때 결정
      · 🎨 톤 결정(iSERI 09-15): **홈은 진한 밴드(톤 1), 안쪽 화면은 흰 바탕 + 굵은 선(톤 2)**.
        세부 색은 나중에 — 교수님 ④ 답 뒤
- [x] **M-2 신청서 마감 전 본인 수정** ✅ 09-16 (D-73) — 범위 **신청서 내용만**, 시점
      **마감 전까지만**. 마이페이지 [신청 내용 수정] → 신청 화면 수정 모드 → PDF 새 버전
      (`신청서-v2.pdf`, 옛 경로는 `pdfHistory`) → 같은 문서 제자리 갱신 → 시트 **같은 줄
      덮어쓰기** + 드라이브 PDF 교체. 규칙: 본인 update 칸 9개로 제한(옛 draft·revision
      조항 대체). 담당자 「수정됨」 배지. FAQ·회원정보 안내 교체. 설계는 `4-기록/01 §4-2`.
      · ✅ 09-16 배포본 시험 완료 (iSERI). 같은 날 후속 **D-74**: 시트 서식(가운데·줄바꿈·
        취소 회색, 열 너비 1회 자동), 상태 열 한글 + 상태 변경·취소 때 같은 줄 재동기화,
        취소 PDF → `01_신청/취소/`. 드라이브는 최신본만(버전은 Storage — 매뉴얼에 명시).
        ✅ 09-17 배포본 시험 완료 (iSERI) — 열 너비 직접 측정 · 링크 셀 · 취소 잠금까지
      · 🔴 **배포 순서**: push 뒤 반드시 `npm run rules` — 규칙을 안 올리면 저장이 거부됨
      · 🔴 **배포본 시험** (체크리스트에 추가): 제출 → 수정 → 마이페이지·담당자·시트·드라이브
        4곳 확인 / 두 번 수정 → PDF v2·v3 / 마감 뒤·선정 뒤 버튼 사라짐 / 시트 머리글에
        Q열 「수정」이 자동으로 채워지는지(첫 동기화 때 머리글을 다시 씀)
- [x] **M-3 처리방침 초상권 절 + 가입 동의서 대조** — ✅ **09-17 전문 초안 완료 (D-77)**.
      `app/privacy/page.tsx` 16개 조문. 값은 `FACTS` 한 곳, **지금 값 = 동의서 값**(3년·1년·
      기한 없음)이라 불일치가 사라짐("법령이 정한 기간" 문구 제거). 초상권 절(제13조) ·
      산출물(제14조) · 국외 이전(제6조 🔴 기관 검토) 추가. 남은 것: **시행일** 오면
      ~~`FACTS.effectiveDate` + `DraftNotice` 제거~~ → ✅ **09-18 게시** (시행일 = 게시일,
      iSERI). 문안은 `lib/privacy/current.ts`, 이전 판은 `versions.ts` + `/privacy/{시행일}`,
      개정 절차는 매뉴얼 §8. 동의서 문구는 안 바뀌어 `CONSENT_VERSION` 그대로.
      이용약관도 **09-18 게시 (D-78)** — 같은 구조 `lib/terms/`
- [x] **M-4 산출물·활동 사진 제출 페이지** — ✅ **09-17 구현 (D-76)**. 대메뉴 「산출물 제출」
      `/outputs` · 참여자 자료실 `/outputs/[id]` · 담당자 `/staff/outputs` · 시트 「산출물」 탭.
      🔴 **push 뒤 `npm run rules`** (firestore·storage 둘 다). 배포본 시험 항목은 작업지시서 D-76 끝.
      · iSERI 검토로 바뀐 것: 제출 항목 이름 없음 · 승인 없음(추가 요청만) · 헤더 대메뉴
      · 「내리기」는 제 판단으로 넣음 — 원치 않으면 뺀다
      · 남은 것: 갤러리(전체 공개)는 자리만 — 산출물 문서에 공개 단계 하나 더 얹는 방식으로 나중에
      *(아래는 설계 이력)* D-19 와 합침. 갤러리 메뉴 정리도 여기 종속.
      ✅ **09-17 다섯 답 확정 (iSERI · D-75)** — 교수님 답을 기다리지 않고 정했습니다:
      · **자유 제출** — 글 + 파일 여러 개, 고정 양식 없음(*"양식만을 위한 것을 만들면
        변수가 생겼을 때 대처가 안 된다"*)
      · **활동 기간 중 아무 때나** — 단 프로그램에 **제출 기간 칸을 지금 넣어 두고**
        비우면 활동 기간을 따르게
      · **신청 단위와 같게** (팀 프로그램은 대표자 1회)
      · **공개 범위 3단계**(담당자만 / 본인+담당자 / 참여자 전체)를 **담당자가 공고
        등록 때 프로그램마다** 지정. 「참여자 전체」는 *"공개 표시된 것을 로그인 회원
        누구나"* 로 **근사**(정확히 선정자만 막으면 규칙이 매번 다른 문서를 읽어야 함)
      · **반려 없음 · 보완 요청만** → 참여자가 수정. **D-73 수정 구조 재사용**이라 쌈
      · 🔴 **사진 속 제3자 초상권** — 제출 화면 안내 한 줄 + 담당자 공개 전 확인 +
        처리방침 근거 한 줄(A-4 에 포함)
      ✅ **참고 사이트 검토 완료 (09-17)** — 「2026 AI 에듀톤 부트캠프 협업 플랫폼」
      (Apps Script). 상세는 `02-답-기다리는-것.md` M-1. 설계에 반영할 셋:
      · **공고마다 「제출 항목」 이름 1~N개**(안 적으면 「산출물」 하나) — 양식을
        박지 않고도 "뭘 내야 하는지"가 또렷해진다. 제출 기한 칸과 같은 자리
      · 공개 목록은 **이름 대신 팀명**(개인 프로그램이면 소속) — **기본값 이름 감춤**
      · 수정 버튼 문구 **「다시 제출하기」**
      안 따라갈 것: **덮어쓰기**(버전 남김 · D-73) · **텍스트 신원**(우리는 로그인)
      보류: 동료 평가(플립북) — 필요해지면 그때
- [x] **M-5 디자인 방향** — ✅ 09-18 **D-81**: iSERI 가 Gemini 와 만든 지시서를 우리 판으로
      적용(Warm Slate 캔버스 + 파랑 버튼 + 청록·귤색 포인트, 홈 6:4 히어로, 목록 가로 카드,
      상세 고정 카드·하단 바, **포스터 기능**). 규칙은 `4-기록/10-디자인-규칙.md` §2.
      *"한 번 바꿔보고 별로면 되돌리자"* — 되돌리는 값은 §2 끝.
      · 🔴 **배포본 시험**: 담당자로 포스터 올리기·바꾸기·지우기 → 홈/목록/상세 ·
        헤더 스크롤 겹침 · 휴대폰 상세 하단 바 · 보완 요청 상자에 주황 배경이 보이는지 ·
        OS 다크 모드로 한 번(자동 전환). 규칙 배포 불필요(포스터는 이미 열린 공개 경로).
      · ✅ 09-18 공용 부품 적용 ② 예약(Button·Badge) · ③ 담당자(모양만 맞춤 — 규칙 §9 참고)
      · iSERI 결정(09-18): **디자인 확정 = 전 화면 PC·휴대폰 훑어 OK** 뒤 개시. 세부 다듬기는 개시 뒤
      · 교수님 한 장 ④(분위기·색) 답이 오면 이 색 위에서 조정

**보존** — 라우트·링크·인증·규칙(취소 4칸 제한)·정산·예약·동기화·폼 검증은 위 항목이
명시한 곳 외에는 손대지 않는다.

# ① 접수 개시 전에 반드시

> 🔎 표시는 **나중에 검토할 항목**입니다. 지금 적용하지 않습니다.

- [x] **D-46 담당자 사유 비공개** — 마이페이지·정산 화면에서 표시 제거,
      담당자 화면 문구를 "담당자만 보는 기록"으로 교체.
      `SHOW_REVIEW_NOTE_TO_APPLICANT` 한 값으로 되돌릴 수 있음
- [x] **갤러리 메뉴 내리기 (09-08 완료)** — 상단 대메뉴에서 뺐고, 홈 화면의
      갤러리 바로가기 칸은 **자주 묻는 질문 · 문의**로 교체했습니다.
      **화면 파일은 지우지 않았습니다** — 산출물이 쌓이면 `Header.tsx` 의
      `NAV` 에 한 줄만 되살리면 됩니다
- [x] **내부 계획 용어가 이용자에게 보이던 것 정리 (09-08)** —
      마이페이지 아래에 `Phase 4′ · 6` / `대기: 갤러리 공개 방침` /
      `산출물 제출 (D-19)` 이 **신청자에게 그대로 떠 있었습니다.**
      뜻을 알 수 없는 글자이고, 자기 마이페이지에서 **아직 없는 기능 목록**을
      볼 이유도 없어 통째로 뺐습니다. `Placeholder` 컴포넌트도 사람 말로
      다시 썼습니다
      *(서식 자료실은 원래 안내 문구가 사람 말이라 그대로 둡니다)*
- [ ] **선정자 목록 공지 — 무엇을 적을지 확정** ⚠️ 개인정보 공개입니다.
      공지사항은 **로그인 없이 누구나** 봅니다.
      · 이름만? 이름 + 학번 뒤 4자리? (동명이인 구분이 안 됩니다)
      · **개인정보처리방침 이용 목적에 "선정 결과 공고"** 를 넣어야 합니다
        → A-4 내용 받을 때 함께 요청
      · 공지를 **회원만 보이게** 하는 기능은 지금 없습니다. 필요하면 만듭니다
- [x] ~~🔎 [검토] 정산 반려 사유만 다시 보이게 할지~~ — **09-13 iSERI 결정: 당장은
      비공개 유지, 반려 사유는 담당자가 직접 연락.** 아래 메모는 나중에 바꾸고
      싶어질 때를 위해 남겨 둡니다 (고치는 법 15분).

      **의견**: 정산 반려 사유는 신청자에게 보여도 될 것 같다.

      **왜 검토할 만한가** — 교수님 답변("사유 비공개, 결과는 공지로")은
      **신청 심사 결과**에 대한 것이었습니다. 정산 반려는 성격이 다릅니다:
      · 심사 결과는 **끝난 통보** — 공지로 갈음할 수 있습니다
      · 정산 반려는 **"이걸 고쳐서 다시 내세요"라는 작업 지시** — 이유를
        모르면 신청자는 **같은 내용으로 다시 제출**하고, 담당자는 같은 건을
        두 번 반려하게 됩니다. 공지로 뿌릴 수 있는 성격도 아닙니다(개인 건)

      **고치는 법 (약 15분)** — 지금은 `SHOW_REVIEW_NOTE_TO_APPLICANT`
      **한 값이 세 곳을 동시에** 좌우합니다. 정산만 떼려면 **상수를 둘로
      나누면** 됩니다:
      · `SHOW_REVIEW_NOTE_TO_APPLICANT` → 신청 심사용 (`false` 유지)
        = `app/mypage/page.tsx` 의 「담당자 안내」 · `app/staff/page.tsx` 문구
      · `SHOW_SETTLEMENT_NOTE_TO_APPLICANT` → 정산용 (`true`)
        = `components/settlement/SettlementSection.tsx` ·
          `app/staff/settlements/page.tsx` 문구·검증 메시지
      담당자 화면 문구가 상수를 따라가게 만들어 두었으므로, **문구는 자동으로
      맞춰집니다.**

      ⚠️ **적용 전에 교수님께 한 번 확인**하는 편이 좋습니다 — "사유 비공개"를
      정하신 분이라, 정산만 예외로 두는 것을 알고 계셔야 합니다

- [ ] **보완 요청·정산 반려 안내 경로** — 사유가 화면에 안 나오므로
      **메일·전화로 알려야** 합니다. 운영 매뉴얼에 절차로 넣기
      *(안 알리면 신청자는 같은 내용으로 다시 제출합니다)*
      *(위 검토 항목이 적용되면 정산 쪽은 이 절차가 필요 없어집니다)*
- [x] **이용자가 읽는 문구 전체 점검 (09-10 완료)** — 결정이 바뀐 뒤에도
      옛 안내가 남아 있던 곳을 찾아 고쳤습니다.
      · FAQ — 임시저장(D-29 없음) · 신분증 제출(D-40 없음) · 보완 사유 표시
        (D-46 안 보임) · 갤러리 공개(D-47 내림) **네 문항**
      · 홈 화면 단계 안내 — "웹에서 작성 · **임시저장**"
      · 사업소개 — "지도안과 활동 사례는 **갤러리를 통해 공유**됩니다"
      · 검색 설명(`layout.tsx`) — "산출물 **갤러리**"
      → 재발 방지 규칙 ⑨ 로 기록
- [x] ~~🔴 **사업소개 내용 전면 교체**~~ — ✅ **09-14 확정 (D-70)**. iSERI 가 준
      계획서 추진체계도(「AI 기본교육 비전 및 추진 체계」)를 분석해 3차 검수 끝에
      **비전 · 인재상(바다인·오름인·바람인) · 목표 3 · 협력체계 5곳**만 싣는 것으로
      정리했습니다. 참여 자격·지원 내용·추진 일정 항목과 페이지 부제는 **없앴습니다** —
      프로그램마다 달라 각 공고가 안내. 지원 대상 "예비교원"은 비전 아래 문단에 녹임.
      임시 문안 표시도 제거.
      · 🔶 **협력체계 로고 파일** — 지금은 기관명 텍스트. 연구소에서 5개 기관 로고
        원본(투명 PNG 또는 SVG)을 받으면 `app/about/page.tsx` 의 `PARTNERS` 를
        `{ name, logo }` 로 바꿔 로고로 교체. 교육청·기상청 로고는 타 기관 것이라
        받을 때 "써도 되는지" 한 번 확인
- [x] ~~이용약관의 갤러리 조항 검토~~ — **그대로 둡니다** (09-12 iSERI). 갤러리는
      준비 중일 뿐 만들 것이고, 무엇이 올라갈지 바뀌면 그때 조항을 고칩니다
- [ ] **약관·개인정보처리방침 실제 내용** — 교수님이 주시기로 함.
      받을 때까지 "초안" 표시 유지

---

---

- [x] **시험 데이터 전체 초기화 — 09-09 1차 완료**
      `node scripts/reset-test-data.mjs <키>` 로 다섯 곳을 한 번에 정리했습니다
      (신청 15 · 열쇠 7 · 정산 3 · 회원 10 · 계정 10 · Storage · 드라이브 사본).
      담당자 계정만 남겼습니다. **접수 개시 직전에 한 번 더** 하게 됩니다
- [ ] 시험용 프로그램(`test-*`) 삭제 또는 `published: false`
      *(09-09 판단: 아직 시험이 남아 지우지 않고 **비공개로만** 내려둡니다.
      `--programs` 는 `test-` 로 시작하는 id 만 지우므로, 초기에 다른 이름으로
      만든 공고는 목록을 보며 손으로 확인해야 합니다)*
- [x] 담당자 권한 ①② 부여 및 교차 확인 완료 (09-05)
- [ ] 🔴 **접수 개시일에 정리 스크립트를 저장소에서 뺀다** (09-09 결정)
      `scripts/reset-test-data.mjs` 는 **실데이터가 들어오기 전에만** 쓰는
      도구입니다. 운영이 시작되면 신청 기록은 **보존 의무**가 있고, 탈퇴도
      삭제가 아니라 상태 변경으로 처리합니다. 남겨두면 언젠가 누군가 씁니다.
      → 마지막 정리를 마친 **그날** 저장소에서 지웁니다.
      필요해지면 깃 기록에서 되살릴 수 있으니 영영 사라지는 것이 아닙니다
- [x] **구글 시트 머리글 교체 (D-43·D-50) — 09-09 완료**
      시험 줄과 **첫 줄(머리글)까지 함께** 지워 두었더니, 다음 제출에서
      새 머리글 17열이 자동으로 들어갔습니다. P열 `프로그램별 기재` 확인.
      ⚠️ **다시 시트를 비울 일이 있으면 첫 줄까지** 지우세요 — 머리글은
      **첫 줄이 비어 있을 때만** 새로 들어갑니다. 옛 머리글을 남기면
      그 아래에 **값이 어긋난 채로** 쌓입니다
- [ ] ⚠️ **옛 회원 문서 처리 (D-43·D-44)** — 시험용으로 만든 회원에는
      `memberType` 이 없고, 반대로 **가입 때 받은 초상권 동의 기록이 남아**
      있습니다. 그 값은 이제 아무도 읽지 않지만(신청서 쪽을 봅니다), 어차피
      시험 계정이므로 위 초기화 때 함께 지우는 편이 깔끔합니다
- [x] **배포본 시트·드라이브 확인 완료 (09-07)** — 새 머리글 15열, 학생·교원
      줄이 유형에 맞게, 초상권 열이 신청 건마다 다르게, 신청 일시 한국 시각,
      PDF 내용까지 전 구간 통과
- [ ] **개인정보처리방침·이용약관 기관 검토** — §1 수집 항목을 실제와 맞게
      고쳤습니다(09-07). 여전히 **보호책임자·연락처·시행일이 "지정 예정"** 이라
      공개 전에 채워야 합니다. 초상권 동의를 신청 단계에서 받는 것도 반영 대상

---

---

## 🟠 A. Firebase — 남은 것

> 프로젝트 생성·Blaze·Firestore(서울)·Storage(서울)·규칙 게시·환경변수 → **완료 (09-05)**

- [x] 지원사업 전용 프로젝트 `iline-edu-support` 생성 (D-25)
- [x] Blaze 요금제 + Firestore/Storage 서울 리전
- [x] 보안 규칙 게시 · 승인된 도메인 등록
- [x] ① Firestore `role: staff` 로 담당자 지정 — 상태 관리 확인 완료
- [x] **예산 알림 설정** — 월 1,000원 / 50·90·100% 알림 (09-05 확인)
- [ ] 예산 알림 **수신 이메일**이 실제로 확인하는 주소인지 점검 — 안 읽는 주소로 가면 없는 것과 같습니다
- [ ] **서비스 계정 키 처리** — ⏸ **시트·드라이브 연동이 끝난 뒤에** 정리하세요.
      그 작업에도 같은 서비스 계정이 필요합니다. 끝나면 이 키 파일은 삭제하고,
      Vercel 환경변수에만 남겨 두는 것이 안전합니다
- [x] ② Custom Claims `supportStaff` 부여 — **다른 계정의 첨부 파일 열람까지 교차 확인 완료** (09-05)
- [x] 규칙을 콘솔이 아니라 **레포 파일 기준으로 관리** — `npm run rules` (09-12).
      `firebase.json` · `.firebaserc` 추가. **PC 에서 `firebase login` 한 번 필요**
      → 팁 문서 「보안 규칙은 push 로 올라가지 않습니다」
- [ ] 🔶 **소유권을 조직 계정으로 이중화** — 현재 Firebase 프로젝트는 **개인 Gmail 소유**입니다
      (공유 드라이브는 조직 계정). 그 계정에 접근이 끊기면 **사업 데이터가 통째로 고아**가 됩니다.
      데이터가 적은 지금이 정리 비용이 가장 쌉니다
      · Firebase 콘솔 → 프로젝트 설정 → **사용자 및 권한** → 조직 계정을 **소유자**로 추가
      · Google Cloud → **결제** → 결제 계정 **권한**에도 조직 계정 추가
        (결제 계정이 끊기면 Storage 가 멈춥니다. 청구액과 무관합니다)
      · 개인 계정을 빼는 것은 나중에 해도 됩니다. **소유자가 둘이 되는 것**이 목적
- [ ] 사업 종료 시 **프로젝트 삭제로 파기** — 절차를 운영 매뉴얼에 명시
- [ ] 🔴 **담당자 업무 기능 마련하기 — 터미널 없이 사이트 안에서** (09-09 제기)

      **목표**: 담당자가 하는 일 중 **터미널·콘솔이 필요한 것을 0으로** 만듭니다.
      지금 사이트 안에서 되는 것: 상태 변경 · 신청 취소 · 정산 승인/반려 ·
      공고 관리 · 공지 관리. **밖에서 해야 하는 것**이 아래입니다.

      | 지금 밖에서 하는 일 | 어디서 | 어떻게 바꿀까 |
      |---|---|---|
      | 담당자 권한 부여·회수 | 콘솔 + 스크립트 | 「담당자 관리」 화면 (아래 상세) |
      | 시험 데이터 정리 | 스크립트 | 접수 개시 후엔 **하면 안 되는 일** — 없앰 |
      | 공고를 콘솔에서 등록 | 콘솔 | 이미 화면에 있음 (부록으로만 남김) |

      **문제**: 지금 담당자를 한 명 늘리려면 **두 곳**을 손봐야 합니다.
      · Firestore 콘솔에서 `role: staff` (화면 권한)
      · `node scripts/grant-staff.mjs <서비스계정키> <이메일>` (파일 열람 권한)

      두 번째가 걸림돌입니다. **서비스 계정 키는 프로젝트 전체를 마음대로 할
      수 있는 마스터키**라, 새 담당자에게 건네주면 그 사람이 전 회원의
      개인정보를 읽고 데이터를 통째로 지울 수 있게 됩니다. Node.js 와
      저장소도 있어야 해서, 비개발자에게는 애초에 불가능합니다.

      **당장의 운영 방법**: 키를 가진 사람(현재 iSERI) **한 명이 대행**합니다.
      새 담당자는 가입만 하고, 권한 부여는 그 사람이 해 줍니다.

      **제대로 된 해법**: 담당자 화면에 「담당자 관리」를 만들어, **이미
      담당자인 사람만** 다른 사람에게 권한을 줄 수 있게 합니다. 서버가
      요청자를 검증하므로 키를 나눠 줄 필요가 없어집니다. 이미 `admin` 을
      쓰는 API 경로(`/api/sync/application`)가 있어 붙일 자리는 있습니다.
      ⚠️ **권한을 주는 기능**이므로 만들 때 특히 조심해야 합니다 —
      마지막 담당자가 자기 권한을 회수해 아무도 못 들어가는 상태 방지 등

---

---

## 🆕 09-08 추가로 들어온 일

- [x] 🎨 **정산 [제출하기] 버튼 세로 위치 맞추기 (09-10 완료)** — 버튼은 이미
      가운데 정렬이었는데 **제목 줄에만 묶여** 있어서, 상자가 두 줄 높이일 때
      위에 붙어 보였습니다. 안내 문구를 왼쪽 칸 안으로 들여 한 덩어리로 묶음
- [x] 🎨 **[수정] 누르면 그 폼으로 화면이 따라가게 (09-10 완료)** —
      `lib/hooks/useRevealForm.ts`. **공고·공지 두 화면 모두** 적용.
      커서는 입력칸이 아니라 **제목**으로 옮깁니다 — 첫 칸에 두면 휴대폰에서
      키보드가 올라와 폼을 가립니다

- [ ] 🎨 **신청서 PDF — 여러 경우로 모양 확인** (09-08 iSERI 제기)
      D-50′ 로 표가 반토막 나는 것은 잡혔지만, **길이가 다른 여러 경우**를
      아직 안 봤습니다. 급하지 않습니다 — 내용이 누락되는 문제가 아니라
      **보기 좋은가**의 문제라, 접수 개시를 막지 않습니다.
      볼 것: · 아주 짧은 신청(팀원, 자유 기재란 없음 — **1장 안에 드는지**)
      · 주제를 아주 길게 쓴 경우 · 첨부 5개 · 자유 기재란이 몇 문단인 경우
      · 이름·프로그램명이 긴 경우(줄바꿈)
      ⚠️ **한 줄이 A4 한 장보다 길면** 어쩔 수 없이 잘립니다(무한 반복 방지).
      실제로 그런 일이 생기는지가 확인 대상입니다

- [ ] 🔴 **상태값 체계를 다시 설계하기 — 선정 절차와 함께** (09-08 제기)

      **문제**: 담당자가 *언제* 상태를 건드리는지가 정해져 있지 않습니다.
      결과를 **공지로 발표**하기로 하면서(D-46) 상태값의 역할이 달라졌는데,
      상태 목록은 그 전에 만든 그대로입니다.

      지금 확인된 것:

      · ✅ **'검토 중' 제거함** (D-49) — 자동으로 안 바뀌고, 공지로 발표하는
        이상 담당자가 100건을 하나씩 켤 이유가 없습니다. 아무도 안 누르면
        **숫자와 필터가 뜻을 잃습니다**
      · ⚠️ **'보완 요청'도 지금은 반쪽입니다.** 상태만 바뀔 뿐
        ① 사유가 신청자에게 안 보이고(D-46)
        ② **신청자가 신청서를 고칠 화면이 없고**
        ③ 고쳤다 해도 **다시 제출하는 동선이 없습니다**
        → 실질적으로 "담당자가 메일로 연락하고 상태만 표시"하는 용도입니다

      **정해야 할 것**

      · 선정 대상을 **어떻게 가려내는가** — 시트에서 추리고 사이트에는 결과만?
        아니면 사이트에서 표시하며 진행?
      · 그렇다면 상태값은 **결과만 담는 칸**인가 (제출 완료 / 선정 / 미선정 /
        취소됨), 아니면 진행도 담는가
      · **'보완 요청'을 살릴 것인가** — 살리려면 신청자 수정 화면이 필요하고,
        버리면 그 안내는 메일로 합니다
      · 선정/미선정을 **한 번에 여러 건** 바꾸는 기능이 필요한가
        (100건을 하나씩 누르는 것은 현실적이지 않습니다)

      ⚠️ **접수 개시 전에 답이 나와야 하는 것은 아닙니다.** 다만 **첫 선정
      작업 전에는** 정해져 있어야 합니다 — 그때 급하게 만들면 100건을 손으로
      누르게 됩니다.

- [x] **신청자 본인 취소 (D-48) — 전 구간 확인 완료 (09-08)**
      보완 요청 상태에서 취소 · 사유 있음/없음 양쪽 표시 · **동시 편집 시
      덮어쓰기 방지(D-48′)** · 선정 후 문의 안내까지 통과.
      보안 규칙 게시 완료
- [x] **'검토 중' 상태 제거 (D-49)** — 버튼·건수·필터에서 확인 완료
- [x] **특정 프로그램용 양식 → 전용 신청 화면 (D-50 · 09-08)** — 세 갈림길 중
      **"양식 항목을 신청 폼으로 옮기기"** 로 결정됐습니다(교수님 지시).
      D-29는 **뒤집지 않았습니다** — 폼 빌더를 만들지 않고, 이 프로그램
      **하나만** 손으로 만들어 `formType` 으로 끼웁니다
- [x] **AI-EDU 연구반 양식 — 표 2개 원본 대조 (09-09 완료)**
      **일정표는 정확**했고, **유형표에서 세 곳**이 틀려 고쳤습니다:
      ① '데이터 과학' 핵심 개념 ② '생성형 AI' 전공 조합에 **'사회' 누락**
      ③ **「예시 활동」 열이 통째로 빠져** 있었음.
      ⚠️ 원본의 `대양광 모듈` 은 **`태양광`의 오타로 보고** 고쳤습니다.
      그대로 두어야 하면 알려주세요
- [ ] **AI-EDU 연구반 공고에 양식 연결** — 공고를 만들 때
      「신청 양식」에서 **AI-EDU 연구반 (2026)** 을 골라야 전용 항목이 뜹니다.
      안 고르면 기본 신청서로 접수됩니다 ⚠️
- [ ] **팀 신청 안내 문구를 공고문에도** — 전용 양식은 구성원이 **각자**
      신청하고 **팀명으로 묶습니다.** 팀명을 한 글자라도 다르게 적으면
      다른 팀이 됩니다. 화면에서도 경고하지만 **공고문에도 팀명을
      정확히 적으라고** 써 주세요 (담당자가 손으로 묶어야 하므로)

---

# ② 접수 개시와 함께

> 🔄 **09-13 변경 — A-1 은 개시 조건에서 빠졌습니다.** AI-EDU 연구반은 일정이
> 급해 **사이트 밖에서 이미 접수 중**입니다(iSERI). 사이트는 **공고 없이** 엽니다 —
> 홈에는 "지금 접수 중인 공고가 없습니다"가 뜨고, 회원가입·시설 예약은 바로
> 씁니다. 다음 프로그램이 생기면 그때 공고를 올립니다(10분). AI-EDU 참여자의
> 정산은 연구소가 따로 처리하고, 사이트에서 받으라는 요청이 오면 그때 방법을
> 정합니다(선정자에게 형식상 신청을 받거나, 담당자 직접 등록 기능을 만들거나).

**공고 등록**은 `docs/1-운영/01-공고-등록-안내서.md` 대로 하면 10분 — 다음
프로그램이 생길 때. 그때 필요한 답은 `02-답-기다리는-것.md` A-1.

---

# ③ 접수 개시 전에 반드시 — 🔴 시설 예약 구현

> **09-10 확정: 시설 예약은 접수 개시 조건입니다** (D-56). 요구사항 정리는
> 09-09~10 에 끝났습니다 — 정책 `4-기록/06`, 운영 `1-운영/06`, 화면 `4-기록/09`.
> 09-09 에 적어 두었던 "무엇을·누가·승인 여부…" 질문 목록은 전부 답이 나와
> 지웠습니다.
>
> **단계마다 로컬에서 시험 → push** 합니다. 한 단계가 끝나기 전에는 다음
> 단계 파일을 만들지 않습니다(반쯤 된 화면이 배포에 섞이지 않게).

### 1단계 — 바탕 + 회원 화면

- [x] 타입 (`lib/types`) · `lib/firebase/reservations.ts` · 공간 상수(`lib/config/venues.ts`) (09-10)
- [x] 보안 규칙 — `support_reservations` · `support_reservation_slots` ·
      `support_reservation_days` · `support_reservation_settings` · `support_reservation_deliveries`
      → 규칙은 `npm run rules` 로 올립니다 (D-63 · 09-12부터. 콘솔 붙여넣기는 예비)
- [x] 날짜 계산 한 곳 (`lib/reservations/window.ts`) — 첫 날짜·끝 날짜·다음 전달일
- [x] `/reserve` 예약하기 (상태 안내 → 공간 탭 → 주간 격자 → 시작+길이 → 좌석 → 확인)
- [x] `/reserve/mine` 내 예약 (취소 포함)
- [x] 상단 메뉴 「시설 예약」
- [x] 콘솔에 규칙 게시 → 로컬 시험 (09-12): 동시 예약 한 명만 성공 ✅ · 하루 2건 거부 ✅ · 취소 후 자리 열림 ✅
- [x] 규칙 다시 게시 → 옛 시험 예약 정리 → 다시 시험 (09-12): 같은 날 다른 공간 ✅ · 같은 시간 다른 공간 ❌ · 휴대폰 ✅

### 2단계 — 담당자 화면

- [x] `/staff/reservations` 행정실 전달 명단 (두 묶음 · 복사 · 전달 완료 · 되돌리기) (09-12)
- [x] 예약 현황 (오늘 기본 · 날짜별 목록 · 담당자 취소)
- [x] 직접 예약 추가 (마감·1건·2시간·주말 무시, 여러 자리, 직접 입력 이름, 바로 확정)
- [x] 예약 설정 (마감 요일·시각 · 전달 요일 · 범위 주 · 휴관일 · 미리보기)
- [x] 규칙 다시 게시 → 로컬 시험 ①~⑤ 통과 (09-12)
      *(원래 항목)* → 로컬 시험:
      ① 회원으로 예약 2건 → 담당자 명단에 뜸 → [전달 완료] → 회원 화면 「확정됨」
      ② 확정된 것을 회원이 취소 → 명단 「취소」 묶음에 뜸 → [전달 완료] → 사라짐
      ③ [마지막 전달 되돌리기] → 접수됨으로 돌아옴
      ④ 직접 추가(주말·3시간·자리 3개) → 현황의 토·일 탭에 뜸 (회원 격자는 평일만이라 안 보이는 게 정상)
      ⑤ 설정에서 범위를 2주로 → 회원 화면 안내가 바뀜 → 다시 4주

### 3단계 — 마무리

- [x] 처리방침 초안에 「시설 예약 및 이용 관리」 목적 + 수집 항목 한 줄 (09-12). **문안 자체는 A-4** — 행정실 전달을 어떻게 적을지, 명단에 연락처를 넣을지 함께 확인
- [x] FAQ 항목 2개 — 「접수됨인데 써도 되나요」 · 「당일·주말·저녁」 (09-12)
- [x] 운영 매뉴얼(00)에 ⚠️ 7번(직접 추가)과 §3′ 목요일 업무 (09-12)
- [x] `scripts/reset-test-data.mjs` 가 예약 컬렉션도 지우게 (09-12 — 열쇠 형식이 바뀌어 당겨서 했음)
- [x] 화면 지도(`1-운영/02`) · 완료 이력 갱신 (09-12)
- [x] 휴대폰 확인 (격자 → 하루씩 보기) (09-12)
- [x] 담당자 화면 넷의 휴대폰 확인 (09-12) — 현황의 요일 탭을 좁은 화면에서 두 줄로, 명단의 예약번호 줄바꿈
- [x] **홈 화면에 시설 예약 바로가기** (09-12) + 소개 문장에 "공부실 예약"
- [x] 🔴 **홈 화면 배너가 가짜 공고를 보여주고 있던 것 수정** (09-12 발견) — "접수중 · D-14 · 2026. 9. 1. ~ 9. 18." 이 **글자로 박혀** 있었고 어디에도 기록돼 있지 않았습니다. 이제 프로그램 목록에서 접수중→예정 순으로 하나를 읽어 보여주고, 없으면 "지금 접수 중인 공고가 없습니다"가 뜹니다. **로컬에서 시험용 공고가 배너에 뜨는지 확인 필요**
- [ ] **가정값을 실제 정보로 바꿉니다** — 🔄 **09-17 격하 (D-75)**: 이 중 개시를
      막는 것은 **「마감·전달 방식(행정실 합의)」 한 줄뿐**입니다. **운영 시간**은
      행정실 통화에 얹고, **취소 시한·문의 창구·범위(주)·요일**은 **개시 후** 설정
      화면에서 조정합니다 — 바꿔도 기존 예약자가 손해 보지 않습니다.
      아래는 그래도 하나씩 확인해서 바꾸거나, 그대로 간다고 확인 표시할 목록입니다.
      목록과 값은 `3-할일/02-답-기다리는-것.md` J 표. 확인 결과는 그 표에
      "✅ 확인" 또는 "✏️ ○○로 변경"으로 남깁니다
      · 마지막 시간 칸 (J-2) · 취소 시한 (J-5) · 마감·전달 요일 · 범위(주) ·
        문의 창구(현장 관리자 번호 여부) · **각 공간 호실**(iSERI 제공 예정 →
        `venues.ts` 의 `room`)
      · **어디를 고치나**: 요일·범위·휴관일 → 담당자 「예약 설정」 화면 /
        운영 시간·마지막 칸 → `lib/config/venues.ts` / 문의처 → `lib/config/site.ts`

---

---

# ④ 접수 개시 이후

- [ ] 🔎 **[검토] 시설 예약 개선안 — 열어 본 뒤 이용 흐름을 보고 판단** (09-12 iSERI 제기)
      · **미디어랩·그룹 스터디룸은 기본 길이를 2시간으로** — 지금은 세 공간 모두
        1시간이 기본이고 [2시간]을 눌러야 합니다. 영상 편집·팀 토론은 2시간이
        보통이라 한 번 덜 누르게 하자는 생각. *"저 혼자만의 생각이라 나중에 고민"*
        (iSERI). 고치는 건 `lib/config/venues.ts` 에 `defaultHours` 한 칸 —
        **일이 아니라 결정의 문제**입니다. 열어 보고 실제로 2시간 선택이 많으면
        바꿉니다. 반대로 기본을 2시간으로 두면 1시간만 쓸 사람이 2시간을 잡아
        자리가 비는 쪽으로 기울 수 있어, 이용률을 보고 정하는 게 맞습니다

- [x] 🆕 ~~**자유 기재란을 여러 개 만들 수 있게**~~ — ✅ 09-22 **D-99 로 완료.**
      담당자가 「칸 추가」로 **글 상자·첨부·동의**를 필요한 만큼 만들고 칸마다 필수 여부를 정합니다.

- [ ] 🆕 **예외 케이스를 어떻게 흡수할까 — 판단 기준 세우기** (09-22 iSERI 방향 지시)
      해커톤의 「유의사항」처럼 **기존 칸으로 안 되는 것**이 앞으로도 나옵니다. 그때마다
      ⓐ 칸 목록을 키울지 ⓑ 전용 양식(코드)으로 갈지 매번 판단해야 합니다.
      · **지금 기준선**은 D-99 의 한 줄입니다 — *"담당자가 정의할 수 있는 것은 「이름 + 안내문 +
        필수 여부」가 붙은 글 상자·첨부·동의 셋뿐. 선택지·분기·계산·형식 검사는 전용 양식."*
      · 🔴 **그 기준선이 깨지는 사례가 두 번 나오면 다시 논의합니다.** 한 번은 예외지만
        두 번이면 규칙이 틀린 것입니다. 나올 때마다 여기에 **사례를 적어 두세요** —
        판단의 근거가 되는 건 의견이 아니라 쌓인 사례입니다
      · **09-25 에 선을 한 번 옮겼습니다 (D-100)** — 「고르기(두 갈래)」가 칸 종류에 추가되어
        이제 **글 상자·첨부·동의·고르기 네 가지**입니다. 남은 선은 **셋 이상 고르기 ·
        분기 · 계산 · 형식 검사**
      · 🔵 **큰 방향** (09-25 iSERI) — *"결국 언젠가는 최대한 전용양식을 만들지 않는 쪽으로
        발전시켜야 합니다."* 새 프로그램에서 안 되는 것이 나오면 **먼저 칸 종류를 늘려서
        풀 수 있는지** 보고, 전용 양식은 마지막 수단으로 둡니다
      · 이미 나온 사례: ① 해커톤 트랙→과제(고르면 다른 목록이 나옴) → 전용 양식으로 감
        ② 데이터 디깅 팀원 2 정보(표) → 한 줄 글 상자 4개로 나눠 흡수함
        ③ 데이터 디깅 팀 대표자/팀원 → **D-100 고르기로 흡수**
        ④ 데이터 디깅 오픈소스 동의함/동의하지 않음 → **D-100 고르기로 흡수**
        (③④ 가 「두 번」이라 규칙대로 선을 옮겼습니다)

- [ ] 🆕 **공고에 「결과 안내」 칸** (09-24 iSERI — *"지금은 운영하는 분들이 그렇게 활용하지
      않을 것 같아 나중으로"*) 신청자가 「검토 중」 배지를 봐도 **"그래서 언제 알 수 있나"** 는
      모릅니다. 그 답은 공고에만 있습니다 — 데이터 디깅 신청서에는 **「10.1.(목) 합격자
      개별연락」** 이 적혀 있는데 **사이트 어디에도 없습니다.**
      · 만들 때: 공고에 글 칸 하나(`cautionText` 와 같은 결) → 마이페이지 신청 카드 아래에 표시
      · 비우면 안 나오게 — 「값이 없으면 칸도 없다」(D-29)

- [ ] 🆕 **「내 것」 화면 세 곳에 카드 부유감 적용** (D-103 에서 일부러 미룸)
      기준으로는 **셋 다 떠야 하는 것**입니다 — 마이페이지 신청 카드 · 내 예약 카드 ·
      내 문의 목록. D-103 에서는 **홈 접수중 카드와 프로그램 목록 카드만** 했습니다.
      · 🔴 **왜 미뤘나** — 마이페이지 신청 카드가 `.card` 클래스가 아니라 **인라인
        `shadow-card`** 다(`app/mypage/page.tsx:180`). 그래서 클래스만 바꿔서는 안 되고,
        **내 예약만 띄우면 같은 사용자의 「내 신청」과 「내 예약」이 서로 다른 무게**가 된다
      · 할 때: 마이페이지 인라인을 `.card-raised` 로 바꾸고, `ReservationCard`(`components/
        reserve/ReservationCard.tsx`)와 내 문의 목록(`components/inquiry/InquiryView.tsx`
        의 `<li>`)을 **함께** 바꾼다. 문의 목록은 접었다 펴는 것이라 `-link` 가 아니다
      · 🔴 **담당자 화면은 건드리지 않는다** — 업무 화면은 목록이 수십~수백 건이라
        부유감보다 **밀도**가 중요하다(D-103 의 판단)

- [ ] 🆕 **그림자 정의가 세 곳으로 흩어져 있다** (D-103 별도 보고)
      tailwind 토큰 넷(`card`·`card-hover`·`raised`·`raised-hover`)과, `app/globals.css` 의
      **`.empty-card` 가 자체 `box-shadow` 를 직접** 들고 있다. D-97 에서 상태 색에 대해
      지적한 것과 **같은 구조 문제**입니다. 급하지 않지만 **새 그림자가 필요할 때
      인라인을 또 늘리지 말고** `.empty-card` 를 토큰으로 옮기는 편이 낫습니다.

- [ ] 🆕 **유지보수 — 10/7 뒤에 할 것** (D-104 파트 1) 접수 중이라 미룬 정리입니다. 대부분 **동작은
      그대로** 두고 정의만 한 곳으로 모으는 것이지만, 신청서·담당자 화면 파일을 열어야 해서
      접수가 끝난 뒤에 합니다(iSERI 09-25 결정).
      · **D1 참여 방식 이름을 한 곳에** — 「개인 신청」「개인」「개인별 신청 (팀 활동이어도 각자)」「단체」가
        목록 카드·홈 카드·상세·담당자 목록에 따로 적혀 있다. 단체 쪽은 `groupNoticeOf` 로 묶였으니
        개인 쪽도 같은 자리로
      · **D2 「신청은 누가」 판정을 `groupEntryOf` 로** — 공고 수정 화면이 공고를 읽을 때
        (`app/staff/programs/page.tsx` 의 toForm)와 저장할 때(`lib/firebase/staff.ts` 의 toInput)가
        `=== 'each'` 라, 셋째 값을 더하면 leader 로 뭉개져 저장된다(D-101 에서 고친 꼴).
        공개 범위 `=== 'members'` 도 같은 꼴
      · **D3 학년 목록 한 벌** — `GRADE_LABEL`(`lib/types`)과 가입·정보 수정 화면의 `GRADES`
        (`components/auth/MemberInfoForm.tsx`)가 같은 목록을 따로 갖고 있다
      · **D4 칸 종류 분기를 표로** — `lib/forms/fields.ts` 의 `if … else` 를 `Record<칸 종류, …>` 로.
        새 종류를 더하면 제출 전 검사·저장·신청서 화면이 조용히 엉뚱하게 받는 것을 빌드가 막게
      · **D5 (화면 글자)** 담당자 「신청 양식」 선택지의 「기본 신청서 (자유 기재란 + 첨부)」 —
        D-99 이후 기본 신청서는 칸 목록이다
      · **D6 (화면 글자)** 대표자 일괄 안내(`GROUP_NOTICE.leader`)의 「신청서에서 동의 여부를
        확인합니다」 — 기본 신청서에는 그런 확인이 없다(담당자가 동의 칸을 만들어야 참)
      · 파트 1 에서 제안했지만 보류(A 등급 — 언제든 가능): `lib/forms` 주석 정리와 「다듬은 값」
        경고 · 칸 종류 빠짐 감지 장치(D4 를 하면 불필요) · 안 쓰는 정의 3개 삭제(파트 2 로)

- [ ] 🆕 **「미선정」과 「취소됨」 다시 보기** (09-25 iSERI — *"확인은 됐는데 제 개인적인
      느낌 때문에 나중에 한 번 더 검토하자. 지금은 그대로"*) **지금 모습은 유지합니다.**
      · 지금: `미처리`(2px 테) > `미선정`(1px 테) > `취소됨`(회색 면만). 09-24 에 둘을
        **서로 맞바꾼 뒤**의 모습입니다 — 그 전에는 취소됨이 미선정보다 더 또렷했습니다
      · 🔴 **이미 두 번 틀렸습니다.** ⓐ 「회색 면이 테두리보다 또렷하겠지」 → 흰 카드 위에서
        `bg-subtle` 면은 거의 안 보임 ⓑ 맞바꾼 뒤에도 **여전히 개운하지 않다**는 평.
        → 다음에 또 감으로 고치지 말고 **둘을 나란히 놓고** 판단하세요
      · 진짜 물음은 색·두께가 아니라 **「이 둘을 굳이 구별해야 하나」** 일 수 있습니다.
        신청자에게 미선정(담당자가 떨어뜨림)과 취소됨(내가 취소함)은 **원인이 전혀 다르지만**
        둘 다 *"끝났고 내가 할 일 없음"* 입니다. 무게를 나누는 대신 **같은 약한 무게 +
        다른 글자**로 가는 선택지도 있습니다
      · 고칠 자리는 한 곳뿐입니다 — `components/ui/Badge.tsx` 의 `TONE` 에서 `closed`·`voided`
        두 줄. 어느 상태가 어느 색인지는 `lib/ui/statusTone.ts` 가 정합니다
      · 함께 보기: 아래 **다크 모드 다듬기** (같은 배지를 다룹니다)

- [ ] 🆕 **다크 모드 다듬기** (09-24 iSERI — *"지금 나쁘지는 않은데 나중에 조금 더 개선하자"*)
      상태 배지(D-97) 작업 중에 다크에서 **나쁘지 않지만 최선은 아니라는** 평. 급하지 않습니다.
      · 볼 곳: **배지 세 단계의 무게**(제출 완료 2px 테 · 미선정 1px 테 · 취소됨 면만) —
        다크에서는 면이 어두워 **테두리가 더 중요해지는데** 그 균형을 라이트 기준으로 잡았습니다
      · 카드·입력칸 테두리(`--border` `--border-strong`)가 다크에서 충분히 보이는지
      · 대비는 이미 통과합니다(선정 12.07:1) — **읽히느냐가 아니라 보기 좋으냐**의 문제

- [ ] 🆕 **시설 예약 안내 문구를 담당자가 사이트에서 직접 쓰게 하기** (09-21 iSERI)
      지금은 코드 상수입니다 — `lib/config/venues.ts` 의 `Venue.notice`(D-94, 2316 화요일
      18:30~21:00 사범대 수업)를 고치려면 **개발자가 배포를 해야 합니다.** 수업 시간표는
      학기마다 바뀌는데 담당자가 못 고치니 곧 거짓말이 됩니다. 담당자가 화면에서 쓰고,
      **어느 위치에 보일지도 고를 수 있게** 하는 것이 목표.
      · **자리는 이미 있습니다** — `ReservationSettings`(Firestore `reservation_settings`,
        화면 `/staff/reservations/settings`, 지금은 마감 요일·휴관일 등)에 안내 문구 칸을
        더하면 됩니다. 새 컬렉션·새 화면을 만들 필요 없음
      · 🔴 **먼저 정할 것**: "임의 위치"의 범위. ⓐ 공간별(2316 고르면 그 공간에만 — 지금
        `notice` 와 같은 자리) ⓑ 예약 화면 맨 위 전체 공지 ⓒ 둘 다. **ⓒ 를 한 번에
        만들지 말고** 실제로 몇 번 고치게 되는지 보고 정하는 쪽을 권합니다
      · 만들 때 함께: 담당자가 쓴 글을 그대로 넣으므로 **줄바꿈 처리**(지금 `notice` 는
        한 줄짜리라 `whitespace-pre-line` 이 없음)와 길이 제한, 비워 두면 안 보이게 하기
      · 코드 상수 `notice` 는 이 기능이 생기면 **제거**합니다 — 두 곳에 두면 어긋납니다

- [x] **영수증을 드라이브 `02_정산` 으로 자동 업로드** + 시트 「정산」 탭 한 줄 —
      09-12 완료 (D-65). 계좌는 안 나감. 🔴 **`DRIVE_SETTLEMENT_FOLDER_ID` 를
      `.env.local` 과 Vercel 에 넣어야 돌아갑니다** (없으면 정산 건마다 「설정 없음」 표시)
- [x] **지급 완료 표시** — 09-12 완료 (D-64). 승인 뒤 담당자가 지급일·메모를
      적고 [지급 완료]를 누르면 상태 `paid`. 신청자 화면에 "○월 ○일 지급되었습니다"
- [ ] 🔴 **예약 목록을 행정실에 전달하는 방법이 정해지면, 그에 맞게 편하게 쓸 수
      있는 시스템 마련하기** (09-13 iSERI)
      지금 명단 화면은 **목록 + [인쇄]** 뿐입니다 — 방법을 가리지 않는 최소 출구로
      남긴 것이고(09-12 "메일 기능은 빼 달라"), 방식이 정해지기 전까지는 그대로 둡니다.
      정해지면 그 방식에 맞춰 한 번에 끝나게 만듭니다:
      · **종이/PDF** → `@media print` 로 서류 모양(제목 「사범대학 공부실 사용 요청」 ·
        전달일 · 담당자 · 공간별 표(날짜·시간·자리·이름·소속·연락처) · 취소 묶음)
      · **메일** → 본문 복사 버튼 또는 서버에서 발송 (받는 주소·서식 필요)
      · **행정실 시스템 입력** → 엑셀/CSV 내려받기
      · **구두·메신저** → 짧은 요약 복사
      무엇이든 **[전달 완료] 누르는 흐름은 그대로**입니다 — 출력 형태만 바뀝니다
- [ ] **정산 기한** — 지금은 선정되면 언제든 낼 수 있습니다. 프로그램에
      `settlementClosesAt` 같은 칸이 필요해지면 그때 추가
- [ ] *(보류)* **총액 칸** — 09-06에 두지 않기로 확정. 담당자가 영수증을 열어
      읽고 합산합니다. 건수가 늘어 부담이 되면 숫자 한 칸을 추가하면 됩니다
- [ ] 계좌 **열람 이력** 기록 — 감사 대비. 지금은 누가 언제 봤는지 남지 않습니다

---

---

> **기능이 다 돌아간 뒤에 합니다.** 지금 디자인을 건드리면 접수 개시가 밀립니다.
> 다만 **인트로 허브(🟤H)를 만들 때 같이 하는 것이 이득**입니다 — 어차피 그때
> 세 사이트의 첫인상을 나란히 놓고 보게 되고, 인트로를 두 번 그리지 않아도 됩니다.

### I-1. UI/UX 개선 (기능은 그대로, 쓰기 편하게)

- [ ] **접수 개시 후 실제 사용 관찰** — 학생들이 어디서 멈추는지 보고 나서 고칠 것.
      지금 짐작으로 고치면 헛수고가 됩니다
- [ ] 신청 흐름 점검 — 로그인 → 프로그램 선택 → 제출까지 **몇 번 클릭하는지**
- [ ] 빈 화면(프로그램 없음·신청 내역 없음) 문구가 **다음에 뭘 하라고** 알려주는지
- [ ] 오류 문구 재점검 — 지금은 개발자 말투가 남아 있는 곳이 있음
- [ ] **시설 예약 — 로그인 전 안내 카드** (09-18 빈 상태 훑기에서) — 지금은 메뉴를 누르면
      설명 없이 로그인 화면으로 바로 넘어갑니다. `/outputs` 처럼 "왜 로그인이 필요한지" 를
      먼저 보여주는 카드(빈 상태 표준 gate 변형)를 두는 쪽이 맞습니다. 개시 전 필수는 아님
- [ ] 🔴 **Vercel 요금제 — 교수님께 여쭙기** (09-18 확인: Hobby 맞음) — iSERI 결정: **지금
      결정하지 않고 Hobby 로 개시.** 왜 여쭙나: Hobby 약관이 "비상업 개인용" 이라 국고 사업
      사이트와 안 맞을 수 있고(걸리면 배포가 멈춤), Pro(월 $20) 결제는 기관 돈이라 제가 정할
      수 없으며, 결제 주체가 정해져야 계정 소유도 옮길 수 있음. 한 장 ⑤ · 답-기다리는-것 M-3
- [ ] **AI-EDU 포스터 다시 올리기** (D-86) — 배포 뒤 담당자 화면에서 같은 파일을 다시 골라
      저장하면 줄어든 크기로 교체됩니다. 옛 40MP 파일은 저장 때 자동으로 지워집니다
- [ ] **터치 태블릿의 hover 잔상** (D-85 검토에서 보류) — `tailwind.config.ts` 에
      `future.hoverOnlyWhenSupported` 를 켜면 탭 뒤 hover 면이 남지 않지만 사이트 전체의
      hover 동작이 바뀌므로 따로 판단. 켜기 전에 `.card-link`·`.touch-target:hover` 도 같이 검토
- [ ] **신청자 쪽 폼에도 같은 오류 표시 방식 적용** — 담당자 화면(09-06)만
      칸 옆 표시로 바꿨습니다. 회원가입·신청서는 아직 맨 위 안내문입니다
- [ ] 모바일에서 표·버튼 크기 재확인 (D-24는 '동작'까지만 확인한 상태)
- [ ] 담당자 화면 — 신청 건수가 늘었을 때도 쓸 만한지 (정렬·검색)
- [x] **회원정보 수정 화면 (09-09 완료)** — 마이페이지 → 내 정보 → [수정하기].
      ⚠️ 만들면서 **보안 규칙의 구멍**을 발견해 함께 막았습니다 — 본인이
      `consents`(동의 이력)·`email`·`status` 를 고칠 수 있었습니다 (D-54)
- [ ] ~~회원정보 수정 화면~~ *(위에서 완료)* — 학년이 올라가거나 학생이
      졸업해 교원이 되면 **본인이 고칠 방법이 없어** 담당자 문의로 옵니다.
      `updateMember()` 는 유형 변경까지 받도록 준비해 두었고, 화면만 없습니다

### I-2. 제주 느낌 반영 (인트로 + 교원양성지원사업)

- [ ] **인트로 사이트** 디자인 — 🟤H 작업과 함께
- [ ] **교원양성지원사업 사이트** 디자인 — 현재는 기능 위주의 기본 스타일
- [x] iLINE 사이트와의 **관계 정하기** — **09-14 교수님: 꼭 통일감을 가질 필요 없다.**
      지원사업은 방향을 새로 잡는다(M-5). 인트로는 그대로 두고, 나중에 맞출지 다시 본다

> **먼저 정해야 할 것** — "제주 느낌"은 사람마다 떠올리는 게 달라서, 이것부터
> 좁히지 않으면 시안을 몇 번씩 다시 그리게 됩니다.
>
> | 정할 것 | 예시 |
> |---|---|
> | 소재 | 바다·현무암·감귤·오름·돌담·바람 중 무엇을 쓸지 |
> | 색 | 바다의 청록 계열 / 현무암의 회흑 계열 / 감귤의 주황 계열 |
> | 톤 | **공공기관다운 단정함** ↔ 지역색이 뚜렷한 개성 |
>
> ⚠️ **국비 사업 사이트**라는 점을 감안해야 합니다. 색이 강하면 공고·서식 같은
> 행정 문서와 어울리지 않고, **본문 가독성**도 떨어집니다. 제주색은 배경·포인트·
> 이미지에 쓰고 **본문은 무채색**으로 두는 편이 안전합니다.
>
> ⚠️ **제주대학교 CI(로고·지정색) 사용 규정**을 먼저 확인해야 합니다.
> 대학 상징색을 임의로 변형해 쓰면 문제가 됩니다.

- [ ] 위 표 3줄을 교수님과 확정
- [ ] 제주대 CI 가이드라인 확인 (로고·지정색·사용 금지 사례)
- [ ] 확정 후 **디자인 토큰(색·간격·서체)부터** 정리 → 화면은 그다음
      *(토큰 없이 화면을 고치면 나중에 색 하나 바꾸는 데 파일을 다 뒤져야 합니다)*
- [ ] 접근성 확인 — 색 대비(KWCAG). 🔵E 항목과 함께

---

---

지원사업 사이트 틀이 갖춰지면 착수합니다. **운영 중인 iLINE 을 건드리는 작업**이라
가장 조심해야 하고, 그래서 마지막입니다.

```
iline.or.kr                    ← 인트로 허브 (iLINE 레포 안에 만듦)
 ├─ iline.or.kr/home           ← 기존 iLINE 랜딩
 └─ aiedu.iline.or.kr     ← 교원양성지원사업  (이미 완성)
```

> ⏸ **09-18 보류 (iSERI)** — 인트로 허브 디자인을 더 개선한 뒤 합치기로 함. 교원양성지원사업은
> **aiedu.iline.or.kr 단독으로 먼저 개시**하고, iline.or.kr 첫 화면은 당분간 기존 그대로.
> 합치는 순서는 접수 개시 체크리스트 §5 에 그대로 있음.
>
> 🔄 **09-13 착수 (D-67)** — 교수님 답을 기다리는 사이에 `intro-hub` **브랜치**로
> 만들었습니다. main 에 합치기 전까지 실서비스는 그대로입니다. 제주 느낌은
> 09-13 iSERI 확정(바다+오름 능선+감귤 포인트 · 바다 청록 · 단정한 톤)대로,
> 연구소 CI 색은 오면 `app/intro.module.css` 맨 위 블록만 바꿉니다.

- [x] 인트로 허브 화면 제작 — `app/page.tsx` + `app/intro.module.css` (09-13)
- [x] iLINE 레포에 인트로를 **루트로** 넣고, 기존 랜딩을 `/home` 으로 이동 (09-13)
- [x] iLINE 내부 `href="/"` **7곳**을 `/home` 으로 (D-21) — Header · admin/layout · login · signup · forgot-password · terms · privacy (09-13)
- [x] **기존 33개 경로는 전부 그대로 유지** — 바뀌는 것은 루트뿐 (D-21)
- [x] Vercel 미리보기에서 확인 (09-13) — 두 카드 링크 · `/home` · 휴대폰 ✅ (관리자 화면은 비밀번호 몰라 건너뜀 — 바뀐 건 링크 한 글자)
- [x] **디자인 일단락 (09-13)** — Gemini 지시서 2건 반영: 상단 두 줄(제목 + 「…교육·지원 플랫폼」),
      카드 규격 통일(주체/이름/무엇/한 줄 · 대상 표시 없음), 능선은 맨 아래로(한라산+오름 3겹+해),
      돌하르방은 시도 후 제거("심령사진 같다"), '맞춤형' 삭제(없는 기능을 약속하는 말)
- [ ] 🔴 **main 에 합치기** — 트래픽 적은 시간대에, Vercel `Promote to Production` 으로 되돌릴 준비
- [ ] 🔎 **인트로의 해를 뺄지 말지 정하기** (09-13 iSERI) — CI 주황이 맡은 유일한 자리. 지금은
      오른쪽 위 구석, 한라산 정상보다 조금 위. 빼면 화면이 청록·파랑만 남아 더 단정해지고,
      두면 제주 소재(바다·오름·해)가 완성됨. 며칠 두고 보고 결정. 빼는 건 `.sun` 한 블록
- [ ] 🔴 **교원양성지원사업 사이트 디자인을 인트로와 통일** (09-13 iSERI) — 지금 지원사업 사이트는
      기능 위주 기본 스타일. 인트로에서 정한 것을 옮긴다: CI 색(청록·파랑 + 대비 확보한
      파생색), 카드 규격(16px · 1px 선 · 부드러운 그림자), 제목/본문 회색 체계, 필요하면 하단
      능선. **토큰(`tailwind.config` 색·간격)부터** 바꾸고 화면은 그다음 — I-2 항목과 합침.
      접수 개시 뒤에 (개시 전엔 기능 확인이 우선)
- [x] 연구소 CI 색 반영 (09-13) — 연구소 CI 5색 + 로고 CI 3색을 `intro.module.css` 맨 위 블록에. ⚠️ 밝은 원색(청록·초록·주황)은 흰 글자 대비 2.5:1 이라 버튼에는 어둡게 내린 파생색을 씀(KWCAG)
- [ ] 🟠B 항목(iLINE 레포 정리) — `.gitignore` 에 `.env*.txt` 는 09-13 추가. **`git rm --cached .env.local.txt`** 는 브랜치 첫 커밋 때 함께. 나머지(middleware · sitemap/robots · API 키 도메인 제한)는 별도

> ⚠️ **iLINE 은 운영 중인 서비스입니다.** 트래픽이 적은 시간대에,
> 되돌릴 준비(Vercel `Promote to Production`)를 해두고 하세요.

---

### iLINE 레포 정리 — 위 작업과 함께

- [ ] **`.env.local.txt` 를 Git 추적에서 제거**
      ```
      git rm --cached .env.local.txt
      ```
      `.gitignore` 에 `.env*.txt` 추가. *(값이 전부 `NEXT_PUBLIC_*` 공개 설계값이라 유출 사고는 아니지만,
      앞으로 서비스 계정 키를 같은 파일에 적으면 그대로 커밋됩니다)*
- [ ] Firebase 콘솔에서 **웹 API 키에 도메인 제한** 걸기
- [ ] **`middleware.ts` 정리** — 선언만 되고 쓰이지 않는 `protectedRoutes` 등 제거,
      matcher가 거의 모든 경로에 걸려 있는데 하는 일이 없음
- [ ] **`sitemap.xml` · `robots.txt` 신규 작성** — 현재 둘 다 없음(404 확인)
- [ ] 루트 페이지 교체 + `href="/"` **7곳**을 `/home` 으로 (D-21, 파일 9개)

---

---

- [ ] Lighthouse — 성능 · SEO · 접근성
- [ ] 웹접근성(KWCAG) 점검 — 공공기관 대상
- [ ] 침투 시나리오 점검 — 남의 신청서·정산 증빙에 접근되는지
- [ ] **담당자 운영 매뉴얼** — 특히 *"시트에서 상태를 고쳐도 사이트에 반영되지 않는다"* 명시
- [ ] **담당자 지정·회수 이력 기록** — 감사 대응.
      회수 시 **세 곳**을 모두 되돌려야 합니다: ① Firestore `role` ② Custom Claims
      ③ **구글 시트·드라이브 공유 해제** ← 사이트에서 막아도 시트로는 계속 보입니다
- [ ] **고아 파일 정리 방법** — 제출이 중간에 실패하면 Storage에 올라간 첨부만 남고
      문서는 안 만들어집니다. 어디에도 연결되지 않은 파일이라 화면에는 안 보이지만
      용량은 차지하고 개인정보이기도 합니다. *(09-05 실제 발생 확인)*
      선택지: ① 주기적 정리 스크립트 ② Storage 수명 주기 규칙 ③ 운영 중 수동 확인
- [ ] 개인정보 보유기간 만료 시 **파기 절차** — Storage와 공유 드라이브 **두 곳** 모두
- [ ] **로그인 유지 정책 결정** — 현재는 Firebase 기본값(브라우저에 저장, 창을 닫아도 유지).
      편의상 맞지만 **공용 PC에서 로그아웃하지 않고 나가면 다음 사람이 그대로 들어옵니다.**
      선택지: ① "로그인 유지" 체크박스로 세션 한정 저장 ② 일정 시간 무활동 시 자동 로그아웃 ③ 현행 유지 + 안내 문구
      *(인증 흐름을 다시 건드리는 일이라 신청서 확정 후에 처리 — 09-05 확인)*

---

---

# ⑤ 완료된 영역에 남은 자잘한 것

## 📄 시트·드라이브 (연동 자체는 완료 · 09-05)

- [ ] **교수님·전산 담당자와 의논** — 외부 공유 정책을 지금처럼 둘지,
      예외 허용을 받아 완전 차단으로 갈지.
      **협의용 문서: `docs/4-기록/03-드라이브-공유-정책.md`**
- [ ] **공유 드라이브 멤버 목록 점검 주기** 정하기 — 학기마다 / 담당자 교체 시
- [ ] 시험 중 생긴 **고아 PDF 정리** — 실패했던 건의 PDF가 `01_신청` 에 남아 있습니다
- [ ] **시각 수정(09-06) 전에 만들어진 파일명·시트 줄**은 자동으로 안 바뀝니다.
      날짜가 하루 이른 이름이 섞여 있으니 시험 데이터 정리 때 함께 없애세요
- [ ] *(나중에)* 신청 건이 많아지면 **프로그램별 하위 폴더**로 나누기 —
      지금은 `01_신청` 한 곳에 쌓입니다. 파일명이 `프로그램명_이름_날짜시각`
      이라 정렬만으로 묶이지만, 수백 건이 되면 폴더 분리가 낫습니다

## 🖥 담당자 화면 (기능 자체는 완료 · 09-06)

- [ ] 공고 등록 시 **[신청 화면 미리보기]** — 지금은 저장 후 확인해야 합니다
- [ ] 프로그램 목록에 **신청 건수** 표시 — 어느 공고가 비었는지 한눈에

## 🌐 도메인 (완료 · 09-08)

- [ ] iLINE 루트 페이지 교체 시점 조율 (④ 인트로 허브와 함께)

---

> 📌 **작업 규칙**(어디서 시험할지 · push 순서 · 되풀이하지 말 것)은
> `docs/2-학습/05-고칠-때-지켜야-할-것.md` 로 옮겼습니다.
> **화면 지도**는 `docs/1-운영/02-화면-지도.md`.
