/**
 * 사이트 공통 문구 — 한 곳에 모아둔다.
 *
 * ✅ 사업 명칭과 문의처가 확정되었습니다 (2026-09-06, 교수님 확인).
 *
 * 여기만 고치면 헤더·푸터·메타데이터·FAQ 문의 섹션이 한 번에 바뀝니다.
 * 화면마다 같은 문구를 적어두지 않는 이유가 이것입니다.
 */

export const SITE = {
  /** 화면에 노출되는 이름 — 헤더 로고, 메뉴, 메타데이터 (09-06 확정) */
  programName: '교원양성기관 교육과정개발 지원사업',

  /** 공문·약관에 쓰는 긴 이름 */
  programNameFull: '교원양성기관 교육과정개발 지원사업',

  /** 사업 총괄 기관 */
  funder: '한국과학창의재단',

  /** 운영 기관 */
  operator: '제주대학교 지능소프트웨어교육연구소',

  /** 소속 대학 */
  university: '제주대학교',

  /** 인트로 허브 (iLINE 레포가 담당하는 루트 도메인) */
  introUrl: process.env.NEXT_PUBLIC_INTRO_URL || 'https://iline.or.kr',

  /** 문의처 (09-06 확정) */
  contact: {
    email: 'ai_edu@jejunu.ac.kr',
    phone: '064-754-1876',
    /** 카카오톡 채널 — 학생들이 가장 편하게 쓰는 경로라 함께 노출한다 */
    kakaoUrl: 'http://pf.kakao.com/_xfxdMxaX',
    hours: '평일 09:00 ~ 18:00',
  },

  /** 개인정보 보유 기간 (사업 종료 후, 년) — 동의서 기준 */
  retentionYears: 3,
} as const

/** 문의처가 아직 등록되지 않았는지 */
export function hasContactInfo(): boolean {
  return Boolean(SITE.contact.email || SITE.contact.phone)
}

/**
 * 담당자가 적은 사유를 **신청자에게 보여줄지** (D-46 · 09-08 교수님 확정).
 *
 * `false` — 사유는 **담당자 내부 기록**으로만 남는다. 선정 결과는
 *           **공지사항에 선정자 목록을 올려** 알린다. 미선정자 목록과
 *           개별 사유는 만들지 않는다.
 *
 * ⚠️ **이 값 하나가 세 곳을 동시에 좌우한다** — 미선정 사유, **보완 요청
 *    사유**, **정산 반려 사유**. 셋이 같은 `reviewNote` 칸을 쓰기 때문이다.
 *    뒤의 둘은 "결과 통보"가 아니라 **신청자가 뭔가를 다시 해야 하는 작업
 *    지시**라, 숨기면 신청자는 무엇을 고쳐야 하는지 알 수 없다.
 *    → 그 안내는 **사이트 밖(메일·전화·카카오톡)** 으로 하셔야 한다.
 *
 * 기능을 지우지 않고 **표시만 끈 이유**: 되돌릴 때 이 값을 `true` 로
 * 바꾸는 것으로 끝나게 하기 위해서다. 담당자 화면의 안내 문구도 이 값을
 * 따라가므로 **"보인다/안 보인다"가 화면과 어긋날 일이 없다.**
 *
 * 🔎 **검토 대기 (09-08)** — **정산 반려 사유만 다시 보이게** 하자는 의견이
 *    있다. 심사 결과는 '끝난 통보'라 공지로 갈음되지만, 정산 반려는
 *    "고쳐서 다시 내라"는 **작업 지시**라 이유가 없으면 같은 내용으로 다시
 *    제출하기 때문이다. 그때는 이 상수를 **둘로 나눈다** —
 *    `SHOW_REVIEW_NOTE_TO_APPLICANT`(신청 심사) 와
 *    `SHOW_SETTLEMENT_NOTE_TO_APPLICANT`(정산). 자세한 절차는
 *    `docs/3-할일/01-남은-일.md` 🟡N 의 검토 항목 참고. **아직 적용하지 않는다.**
 */
export const SHOW_REVIEW_NOTE_TO_APPLICANT = false
